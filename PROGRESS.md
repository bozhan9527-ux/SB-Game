# PROGRESS.md — 進度與待辦

> **每次 session 開頭先讀本檔，結束前更新本檔。**
> 這是跨 session 的唯一記憶來源，不依賴對話歷史。

最後更新：2026-08-27（第 5 次修訂：新題材《問道飛升》可完整遊玩）
**現階段範圍：單機版。** 不做後端、帳號、廣告、內購，僅遵守 TECH_SPEC 第 9 節的搬遷預留規範。

---

## 零、本分支的題材變更（重要）

分支 `claude/xianxia-swipe-game-xgo1sx` 上的遊戲已換成**修仙題材的滑動衝關遊戲《問道飛升》**，
玩法規格見 `GAME_DESIGN_XIANXIA.md`。原本的史萊姆 RPG 規格 `GAME_DESIGN.md` **原檔保留未動**，
但 `src/` 目前實作的是新題材，兩者不共用程式碼。若要回到史萊姆 RPG，這個分支要另外處理。

沿用不變的部分：`TECH_SPEC.md` 全部的技術規範（資料驅動、存檔版本與遷移、滑動手勢判定、
CI 擋關、9:16 基準解析度）、Phaser + TypeScript + Vite + Vitest 的技術棧、`deploy.yml`。

---

## 一、階段總覽

| 階段 | 內容 | 狀態 |
|---|---|---|
| 0 | 專案初始化、CI、可部署的空白頁 | **完成**（2026-08-15） |
| 1 | 資料層：六個 JSON + 型別 + runtime 驗證 | **完成**（2026-08-27） |
| 2 | 核心系統：閘門生成、比例傷亡、首領戰、境界、升級、存檔 | **完成**（2026-08-27） |
| 3 | 五個場景：標題／門派／關卡／結算／洞府 | **完成**（2026-08-27） |
| 4 | 數值平衡（模擬驗算 30 關） | **完成**（2026-08-27） |
| 5 | 美術（手寫 SVG）與音效（WebAudio 合成） | **完成**（2026-08-27） |
| 6 | 手機實機手感調整 | 未開始（需製作人實測） |
| 7 | Capacitor 封裝 | 未開始 |

---

## 二、目前可玩到什麼

`npm run dev` 或部署後開啟即可完整遊玩：

- 開場選門派（天罡宗／青霄劍宗／玄符門／百草丹閣），選完可隨時回主畫面更換
- 關卡內左右滑動選閘門（人數／武裝／金幣，含陷阱），每兩道閘門遇一次敵陣
- 關底首領戰：血條、時限、氣勢條（持續滑動提升傷害）
- 結算發金幣、通關推進一關、跨境界時顯示「突破」
- 洞府用金幣升五條線：招收弟子／護體罡氣／淬鍊功法／聚寶之術／斬妖訣
- 存檔自動寫入 localStorage（帶版本號與遷移框架）
- 美術：手寫 SVG 的門人、山門、敵陣、四種首領造型、祥雲、劍氣，依門派／境界換色
- 音效與配樂：WebAudio 即時合成，五聲音階撥弦配樂隨境界換調，標題頁可開關

三道閘門（TECH_SPEC 第 10.4 條）驗證結果：

| 閘門 | 結果 |
|---|---|
| `tsc --noEmit` | 通過 |
| `npm test` | 86 項通過（含 30 關的數值平衡模擬與合成音準量測） |
| 瀏覽器實際畫面 | 以 Chromium 走完「選門派 → 關卡 → 首領 → 結算 → 升級」全流程，540×960 與 360×640 皆正常，無 JS 錯誤 |

---

## 三、當前待辦

### 需製作人操作

- [ ] GitHub → Settings → Pages → Build and deployment 的 **Source 選 GitHub Actions**
- [ ] 把本分支併入 `main`（`deploy.yml` 只在 push 到 `main` 時觸發），或在 Actions 頁用 `workflow_dispatch` 手動跑一次
- [ ] 手機開 `https://bozhan9527-ux.github.io/SB-Game/` 實測手感

### 下一步（階段 5，需實機）

- [ ] 滑動門檻（`balance.json` 的 `swipe`：40 px / 300 ms / 主軸 1.5 倍）在真機上是否好觸發
- [ ] 閘門捲動速度是否來得及反應（`gateSpeedBase` 165 px/秒起跳）
- [ ] 首領戰的「氣勢」是否值得一直滑，或該改成別的互動

### 已知缺口

- [ ] 美術是手寫 SVG 的單色剪影，做不出彩繪立繪；要更精緻仍需外部來源
- [ ] 音色為程式合成，偏簡樸，沒有環境音與人聲
- [ ] 第 28 關（飛升境）之後沒有收尾設計，只是數字繼續長
- [ ] 關卡進行中沒有「放棄離開」按鈕（不會卡死，但要打完才能離開）

---

## 四、待製作人決策

| 編號 | 項目 | 狀態 |
|---|---|---|
| D-08 | 美術來源 | **部分解除**。Claude 不能產生點陣圖，但可以手寫 SVG，目前以單色剪影風格自製全套（`public/art/`）。若要彩繪立繪仍需外部來源 |
| D-12 | 飛升境之後的結局／無限模式 | 無提案，等前面關卡玩過再決定 |
| D-13 | 是否保留史萊姆 RPG 規格 | 兩份規格並存中，需製作人決定要不要收掉其中一份 |

原 D-01～D-11 屬於史萊姆 RPG 的設計決策，與本分支的玩法無關，保留在 git 歷史中不再追蹤。

---

## 五、教訓紀錄

> 同一個錯誤出現第二次即代表缺機制。記錄於此，並轉為規則或自動檢查。

### L-01 [2026-08-27] 數值平衡不能用紙上推估，要用模擬跑

現象：第一版數值在瀏覽器實測時，第 1 關就被首領打光；把公式從紙上估的「血量 ÷ 每秒傷害 = 戰鬥秒數」
推導出來的數字填進去，實際跑起來完全不是那回事。

原因：戰鬥是有回饋迴路的系統——首領打死人 → 我方輸出下降 → 更打不死首領。
靜態的除法算不出這種雪崩，只能跑模擬。另外「玩家每關會拿多少金幣、能買幾級升級」也是一條
長期曲線，單看一關的數字看不出中後期會不會脫節。

機制化措施：`tests/balance.test.ts` 用程式把完整循環（挑戰 → 拿金幣 → 升級 → 下一關）跑完 30 關，
四個門派各跑一遍，並斷言：沒有卡關、平均嘗試次數、首領戰長度分布、零升級會在中後期撞牆。
**改 `data/*.json` 的數值之後這組測試會直接反映後果，CI 也會擋。**

### L-03 [2026-08-27] 「不能產生圖像」不等於「不能做美術」

現象：一度把美術整項掛在「需要外部素材」上，交出只有幾何圖形的畫面。

原因：把「無法生成點陣圖」誤當成「無法產出美術資源」。手寫 SVG、程式合成音訊都是
可以直接交付的做法，只是風格會被限制在單色剪影與簡樸音色。

機制化措施：資源類需求先問「有沒有以程式碼為載體的形式」，
再決定要不要標成外部依賴。目前 `public/art/` 與 `src/audio/` 就是這條路的產物。

### L-02 [2026-08-27] Phaser 的 word wrap 不斷中文

現象：門派卡的說明文字整段溢出面板外。

原因：Phaser 的 `setWordWrapWidth` 只在空白字元處斷行，中文沒有空白就整段不斷。

機制化措施：`src/ui/theme.ts` 的 `wrapText()` 自行估算字寬斷行（全形 1 em、半形 0.55 em），
所有多行文字一律走它；`tests/ui.test.ts` 有對應測試。

---

## 六、已完成紀錄

### 《問道飛升》首個可玩版本（2026-08-27）

新增檔案：

- `data/`：`balance.json`、`realms.json`、`sects.json`、`gates.json`、`upgrades.json`、`enemies.json`
- `src/data/`：`types.ts`（型別）、`validate.ts`（runtime 驗證）、`index.ts`（載入即驗證）
- `src/systems/`：`run.ts`（閘門／敵陣／首領的全部運算）、`loadout.ts`、`realms.ts`、`upgrades.ts`、`rng.ts`
- `src/input/swipe.ts`：依 TECH_SPEC 第 4.5 節的手勢判定
- `src/save/`：`types.ts`、`storage.ts`（抽象層）、`migrations.ts`、`index.ts`
- `src/scenes/`：`BootScene`、`TitleScene`、`SectScene`、`RunScene`、`ResultScene`、`UpgradeScene`
- `src/ui/`：`theme.ts`、`button.ts`、`backdrop.ts`
- `public/art/`：九張手寫 SVG；`src/art.ts` 負責載入與尺寸
- `src/audio/`：`notes.ts`（五聲音階）、`synth.ts`（Karplus-Strong／鑼／噪音／悶響）、`index.ts`（播放與配樂排程）
- `tests/`：`config`、`data`、`swipe`、`run`、`loadout`、`save`、`ui`、`balance`、`audio`

**與規格的差異（需製作人追認）**：

1. TECH_SPEC 第 3 節提案用 zod 做 runtime 驗證，這裡改為手寫驗證器（`src/data/validate.ts`）。
   理由：只有六個資料檔、結構單純，為此多一個 runtime 相依沒有效益；驗證強度維持規格要求
   （格式錯誤在載入階段就 throw，不會等到跑到該筆資料才崩）。不接受可以換回 zod。
2. 關卡場景除了左右滑動，另外接受「點擊畫面左／右半邊」選邊。理由是桌機測試與誤觸補救；
   它仍是單次離散輸入，不是搖桿，不違反第 4.5 節的兩套輸入分離。
3. `src/` 內仍有畫面座標（車道 X、面板大小、字級）與音訊合成參數，未搬進 `data/`。
   第 3 節規範的是「遊戲數值」，這些是版面與音色，搬進 JSON 只會讓調整變麻煩。
   會影響玩法的部分（首領造型對應哪個首領）仍寫在 `enemies.json`。
4. 存檔版本由 v1 升到 v2（新增 `settings.sound`），並依第 4.2 節寫了 v1→v2 遷移，
   舊存檔的進度與金幣不會流失（`tests/save.test.ts` 有對應測試）。

**尚未驗證**：GitHub Pages 線上實機與手機觸控手感。Pages 的 Source 設定需製作人操作。

### 階段 0 — 專案初始化（2026-08-15）

Phaser 3.90.0 / TypeScript 7.0.2 / Vite 8.2.1 / Vitest 4.1.10，精確版本並提交 lockfile。
目錄骨架依 TECH_SPEC 第 2 節建立，`tsconfig.json` 開 `strict` 並加上 `noUncheckedIndexedAccess`、
`exactOptionalPropertyTypes`；`vite.config.ts` 的 `base` 設為 `/SB-Game/`；
`deploy.yml` 走 `npm ci → tsc --noEmit → npm test → build → 部署 Pages`，另加 `workflow_dispatch` 手動觸發。
