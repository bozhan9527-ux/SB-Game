# PROGRESS.md — 進度與待辦

> **每次 session 開頭先讀本檔，結束前更新本檔。**
> 這是跨 session 的唯一記憶來源，不依賴對話歷史。

最後更新：2026-08-29（第 19 次修訂：算出並釘住陣法的天花板——七條，八條不可能）
**現階段範圍：單機版。** 不做後端、帳號、廣告、內購，僅遵守 TECH_SPEC 第 9 節的搬遷預留規範。

---

## 一、階段總覽

| 階段 | 內容 | 狀態 |
|---|---|---|
| 0 | 專案初始化、CI、可部署的空白頁 | **完成**（2026-08-15） |
| 1 | 資料層：六個 JSON + 型別 + runtime 驗證 | **完成**（2026-08-27） |
| 2 | 核心系統：閘門生成、比例傷亡、首領戰、境界、升級、存檔 | **完成**（2026-08-27） |
| 3 | 五個場景：標題／門派／關卡／結算／洞府 | **完成**（2026-08-27） |
| 4 | 數值平衡（模擬驗算 30 關） | **完成**（2026-08-27） |
| 5 | 美術（手寫 SVG）與音效（WebAudio 合成） | **完成**（2026-08-27） |
| 5b | 操作改為觸控跟隨、閘門去底色、敵我單位動畫化 | **完成**（2026-08-27） |
| 5c | 境界擴為每境界九層、關卡加長、九款人物造型 | **完成**（2026-08-27） |
| 5d | 人物全彩化、走路兩幀動畫、首領重新配色 | **完成**（2026-08-27） |
| 5e | 妖獸依名字分成六種、門人造型隨境界分三階 | **完成**（2026-08-27） |
| 6 | 四角度審視與修正（玩家／UI／遊戲設計／美術） | **完成**（2026-08-27） |
| 6b | 補上四項延後項目：門派被動、首領戰取捨、成就、境界地貌 | **完成**（2026-08-27） |
| 6c | 參考影片：陣前齊射、連擊倍率、浮動傷害數字、首領血條上移 | **完成**（2026-08-28） |
| 7 | **玩法轉型：閘門跑酷 → 法寶符卡牌塔防**（製作人指定，比照參考影片） | **完成**（2026-08-28） |
| 7b | 新手教學（互動三步）與玩法說明頁 | **完成**（2026-08-29） |
| 7c | 陣法：橫／縱／斜連線加成，讓陣位的位置有意義 | **完成**（2026-08-29） |
| 7d | 修正「首領跑掉也算通關」；陣法改為不同種才成陣 | **完成**（2026-08-29） |
| 6 | 手機實機手感調整 | 未開始（需製作人實測） |
| 7 | Capacitor 封裝 | 未開始 |

---

## 二、目前可玩到什麼

`npm run dev` 或部署後開啟即可完整遊玩：

**核心玩法**

- 妖魔分五波由上而下推進，走到山門就扣耐久，耐久歸零即失守
- 手牌的**法寶符**放到陣位上會自動朝上出手；**同種同階疊起來合成高一階**，傷害指數成長
- **拖放或點兩下都可以**：能合就合、空位就搬、否則互換；拖到最下緣（或選起來後點那裡）棄符
- **階數上限每三關 +1**，抽到的符固定比上限低四階——抽符速度就是成長速度
- **陣法**：一整條線全同種（金色虛線）或全不同種（綠色實線）都會成陣，橫加傷害／縱加出手／斜要 3×3
- **妖魔習性**：甲（每發被削一截）／疾（走得快）／裂（死了裂成兩隻），血量都打過折——
  改變的是「誰打牠有效率」，不是「大家都打不動」
- 關底首領血條在畫面頂端，撞進山門扣六倍耐久，60 秒時限；**沒斬掉就不算通關**

**帶進場的選擇**

- 二十張符裡帶四張入場，那四張就是整場的抽符池；推關解鎖新的符
- 符籙譜可**排序**（每秒輸出／出手快慢／道數）、**篩選**（單體／多目標／控場／增益）、
  **並排比較**最多三張——「帶哪四張」是唯一的 build 決策，它需要能被推理
- 四個門派各專精一種符——沒帶進場專精就不生效，兩個畫面互相顯示這件事
- **門派修為**：用同一派通關會累積該派的修為（每 5 場升一階，最多四階，每階 +4% 傷害）；
  換派要付金幣，但舊修為留在原地，回去就還在
- **試煉**：五條自我設限的條件（不合成／獨門一符／孤身守門／一夫當關／速斬），
  獎勵只有金幣倍率
- 洞府用金幣升六條線：聚眾成軍／御器訣／淬鍊功法／引靈訣／聚寶之術／陣法擴充

**長期進度**

- **輪迴轉世**（第 82 關之後）：把深度換成跨世的仙緣點，關卡與金幣歸零，
  但門派修為、符籙解鎖、成就與紀錄都留著
- 仙緣四條線：道基永固／夙世積財／護山大陣／宿慧未泯（階數上限 +1）
- **戰報**：每張符的傷害貢獻、陣法整場平均加成、輸出曲線
- **閉關**：離線最多累積八小時的金幣，速率約是實際遊玩的四分之一
- **個人紀錄**與**存檔碼**（匯出／匯入，帶校驗碼）
- 十九項成就；存檔自動寫入 localStorage（版本 11，帶完整遷移鏈）

**操作與呈現**

- 暫停、1×／2×／3× 變速（記進存檔）
- 受擊白閃與擊退、倒下動作、出手預備、合成的分身飛行與爆光、重擊凍格
- 九個場景之間一律 220ms 黑場轉場
- 第一次進第 1 關會跑**互動教學**；之後每隔幾關補一則單頁課程，取代文字牆
- 美術：全彩 SVG 的門人／兵卒／首領／山門；音效與配樂為 WebAudio 即時合成，
  四小節一段的 I–V–vi–IV 骨架，隨境界轉樂句起點

三道閘門（TECH_SPEC 第 10.4 條）驗證結果：

| 閘門 | 結果 |
|---|---|
| `tsc --noEmit` | 通過 |
| `npm test` | 233 項通過（含 81 關的數值平衡模擬、習性設計意圖、存檔碼與遷移） |
| 瀏覽器實際畫面 | 以 Chromium 走完全部九個場景與教學／首領／結算／戰報／轉世流程，405×720 正常，無 JS 錯誤 |

---

## 三、當前待辦

### 已上線

`https://bozhan9527-ux.github.io/SB-Game/` 於 2026-08-27 部署成功（run #7）。
`main` 已是預設分支，之後推到 `main` 就會自動重新部署。

### 下一步（需實機）

製作人於 2026-08-29 回報「目前都符合預期」。以下六項原本掛在這裡，
四項已被這一輪的改動取代，兩項改為持續觀察：

- [x] 拖曳判定範圍 —— 已改成「取最近的一格 + 合成優先（MERGE_SLACK）」，
      並補上點兩下的替代操作（U-03），舊的軸向容差寫法已不存在
- [x] 波次節奏 —— 已補 1×／2×／3× 變速與暫停（U-01、U-02），節奏由玩家決定
- [x] 教學節奏 —— 教學支援點兩下，且前兩步會把戰鬥停住
- [x] 陣法連線的辨識度 —— 已加上虛線／實線與節點形狀的差異（U-05），
      並在每一格常駐顯示該格吃到的倍率
- [ ] 小螢幕（360×640）上手牌五格 × 陣位九格是否還按得準
      —— 目前僅在 405×720 驗過；按鍵熱區一律 ≥ 44px，但實機仍需抽查
- [ ] 線上部署後的畫面與音效
      —— 開發沙箱的網路代理擋掉 `github.io`，Claude 無法自行確認，只能由製作人回報

### 已知缺口

- [ ] 美術是手寫 SVG 的扁平風格，做不出彩繪立繪；要更精緻仍需外部來源
- [ ] 音色為程式合成，偏簡樸，沒有環境音與人聲
- [ ] 沒有任何遙測：留存、流失關卡、玩家實際帶哪四張符，全部只能靠猜
- [ ] 抽符完全不可控（H-03）：沒有選牌、重抽、鎖定
- [ ] 雲端存檔與排行榜需要後端，目前只有本機替身（存檔碼與個人紀錄）
- [ ] 零留存鉤子與零傳播（O-01／O-03）：無每日任務、無分享、無好友
- [ ] 數值改動要重新部署（O-04）：沒有遠端配置
- [ ] v3 舊存檔的「斬妖訣」等級不會退費（該升級線在新玩法裡不存在）

---

## 四、待製作人決策

| 編號 | 項目 | 狀態 |
|---|---|---|
| D-08 | 美術來源 | **部分解除**。Claude 不能產生點陣圖，但可以手寫 SVG，目前以單色剪影風格自製全套（`public/art/`）。若要彩繪立繪仍需外部來源 |
| D-12 | 飛升境之後的結局／無限模式 | **已處理**。第 82 關之後可以「轉世重修」，把深度換成跨世的仙緣點（見 `src/systems/karma.ts`） |
| D-13 | 要不要架一個後端 | 未決。雲端存檔（H-05）與排行榜（H-04）都需要它；目前分別以存檔碼與個人紀錄替代 |
| D-15 | 掃蕩／自動戰鬥（H-01） | **決定不做**。這一場的勝負來自即時排陣：模擬 AI 在第 140 關勝率 100%，1500ms 操作速度的真人只有 25%。掃蕩＝跑模擬拿結果，會比親手打更強，等於把陣法與合成層變成可選的。改以「閉關」消掉真正的體力活（回頭重刷舊關） |
| D-14 | 陣法與合成的張力（P-05） | **刻意保留現狀**。同色也能成陣是製作人明確要求的（後期太硬），代價是少了一個決策。要改回互斥就等於推翻那個決定，需要製作人重新拍板 |

原 D-01～D-11 屬於舊題材的設計決策，已隨規格一併移除，不再追蹤。

---

## 五、教訓紀錄

> 同一個錯誤出現第二次即代表缺機制。記錄於此，並轉為規則或自動檢查。

### L-30 [2026-08-29] 「品類標配」不等於「這款遊戲該有」——先問它會不會取代玩法

審查把「沒有掃蕩／自動戰鬥」列成高影響，理由是塔防／放置類都有，
而且模擬 AI 的邏輯早就寫好可以直接拿來用。第一版建議照做，是製作人擋下來的：
「這個遊戲掃蕩就不用玩了啊。」

他是對的，而且這個 session 自己就量過證據：**模擬 AI 在第 140 關勝率 100%，
同一組數值下 1500ms 操作速度的真人只有 25%**（見 L-18）。
在勝負來自即時操作的遊戲裡，掃蕩不是「省時間」，是「比親手打更強」——
它會讓陣法與合成整層變成可選的。同一個功能在 build 決定勝負的遊戲裡完全沒問題，
差別不在功能，在這款遊戲的勝負由什麼決定。

真正該消掉的體力活也不是戰鬥本身，是「回頭重刷已經通關的關卡換金幣」。
換成閉關（離線收益）就解掉了，而且完全不碰技術門檻。

教訓：**引進一個品類標配之前，先問「它會不會讓玩家不必做這個遊戲最核心的那件事」。**
會的話，該做的是找出那個功能想解決的痛點，另外想一個不碰核心的解法。

### L-29 [2026-08-29] 給敵人加「習性」時，血量必須跟著打折，否則加的是難度不是行為

妖魔原本只有一種行為：直線往下走、有血量。這是二十張符選擇空間窄的根源——
面對只會走路的目標，二十張符全部塌成同一個問題「每秒打多少」。

第一版直接加了護甲（每發削掉一截）、疾行（走得快）、分裂（死了裂成兩隻），
血量原封不動。結果全輔助那一副牌組**直接卡死在第 78 關**：
護甲對它等於一律扣掉三成五的輸出，而它本來就在及格邊緣。
把護甲的數值一路調弱到幾乎沒感覺，還是卡在同一關——因為問題不在強度，在形狀。

真正的修法是**血量打折**：護甲 ×0.62、疾行 ×0.85、分裂母體 ×0.6。
總工作量與素面的一隻相當，換掉的只是分佈——護甲讓大單發划算、分裂讓範圍傷害划算。
調完之後全部符籙配置都推得完 81 關，而且不同牌組面對不同習性真的有高下之分。

另一半同樣重要：**每個境界只給一種妖魔習性，另一種維持素面**。
兩種都帶的話，整關沒有任何一波是「照常打」的，難度就是一整層地墊高。

教訓：**「行為」與「難度」的分界線是總工作量。**
如果加了一個特性之後打死它要花的總力氣變多了，那加的是難度；
只有總力氣不變、而「誰打它比較有效率」變了，加的才是行為。
這條規則已寫成 `tests/traits.test.ts`：若哪天調到「大單發和多小發面對護甲一樣好」就會紅。

### L-28 [2026-08-29] 給玩家的「選擇」若沒有代價，它就不是選擇，是選單

門派原本是遊戲裡唯一的 build 身分，卻不需要任何承諾：換派完全免費、
沒有任何門派專屬的長期進度，而成就「四道皆通」還獎勵兩萬金鼓勵你切換。
結果門派不是身分，是一個隨時可改的修飾選單——選錯沒有代價，選對也沒有回報。

補的是**修為留在門派身上**（分派記通關次數，換派帶不走）加上**換派要付金幣**。
但關鍵的第三條是：**修為不會被沒收**。離開時留在原地，回來就還在。
若換派會把修為清掉，那就不是「承諾」而是「懲罰」——玩家會因為怕虧而永遠不敢嘗試，
四個門派等於只剩第一次選的那個，比原本更糟。

同一條規則在轉世（P-04）上也成立：轉世歸零關卡進度與金幣，
但**不動符籙解鎖與成就**。收回玩家已經打出來的選擇，只會讓人不敢轉世。

教訓：代價要落在**資源**上（金幣、時間、進度），不能落在**已經取得的選擇**上。
前者讓決定有份量，後者只會讓人不敢決定。

### L-27 [2026-08-29] 已經存在的取捨被藏起來，比沒有取捨還糟

青霄劍宗的被動是「劍陣符 +35%」，而最優牌組不含劍陣符——
劍修帶最優牌組，被動整場歸零，而畫面上一個字都沒有。
兩個畫面互不知情：選門派頁提到符零次，符籙譜提到門派零次。

這比「沒有取捨」嚴重：玩家連自己選錯了都不知道，只會覺得「劍修怎麼比較弱」。
修法不是加機制，是把已經存在的那條線畫出來——
選門派頁逐派標「專精 X・已帶／沒帶」，符籙譜頂端常駐一行門派專精狀態。

教訓：做完一個系統之後，要問的不是「還缺什麼機制」，
而是「**現有的機制裡，有哪一條玩家沒有辦法從畫面上看出來**」。
看不出來的規則等於不存在，而且比不存在更糟——它會讓玩家對自己的判斷失去信心。

### L-26 [2026-08-29] 自我設限的玩法，獎勵只能是「可以用時間換」的東西

試煉（挑戰條件）的價值在於「我自己給自己找事做」。
只要它給的東西無法用一般玩法取得——專屬的符、專屬的升級、專屬的關卡——
它就從一個選項變成一份作業，正好毀掉它存在的理由。

所以獎勵一律只是金幣倍率：金幣可以用時間換，開試煉永遠只是「換一種玩法」
而不是「必須這樣玩」。這條寫成了測試（資料只允許那六個欄位），
避免日後在挑戰上掛一個「限定符籙」而不自覺。

同一條規則約束了仙緣（轉世點數）：它只給乘區，不解鎖任何內容。
否則不轉世的人會落後在「內容」而不只是「速度」上。

### L-25 [2026-08-29] 高頻事件的回饋要節流或累積，否則回饋更多、資訊更少

補打擊感時最直覺的寫法是「每次命中都跳一個傷害數字、響一聲」。
實際跑起來，後期一秒有幾十次命中，畫面變成一片閃爍的小數字、聲音變成持續的白噪音——
**回饋加了，可讀的資訊反而少了**：玩家看不出這一發打了多少，也聽不出有沒有打中要害。

三種處理方式，分別對應三種事件：

- **累積**（傷害數字）：每隻妖魔累積 320ms 再吐一個總數。數字少了但大得起來，
  而且打掉四分之一血以上的那種會用金色大字——強弱一眼分得出來。
- **節流**（命中音）：最多每 90ms 一次，音量壓到 0.16。它的作用是觸感不是提示。
- **分階**（山門破損）：只在跨階的那一刻重畫，不是每掉一點耐久就重畫。
  裂痕的位置由序號算出來而不用亂數——會跳動的裂痕看起來像雜訊，不像損壞。

教訓：**回饋的價值不在次數，在對比。** 每一次都給等於每一次都不給；
要讓玩家看出「這一下比較重」，就得讓大部分的下不那麼響。

### L-24 [2026-08-29] 說明頁是寫給已經懂的人查的，教不會還不懂的人

製作人看完玩家實測後說：「玩法說明是不是可以設計到境界裡面做成新手教學，
不然**文字那麼多沒有人會看**。」

那一頁我寫得很仔細：八個章節、每個數字都從 `data/*.json` 讀、還做了捲動。
問題不在品質，在**時機**——它把八條規則同時攤在一個還沒有任何一條規則派得上用場的人面前。
玩家實測的回饋正好印證了：他玩完之後只學到「湊不同的圖案成一線會有 buff」，
其他七章等於沒發生。

改法是把規則拆成十課，綁在「它第一次派上用場的那一關」講，每條一個標題兩行字。
第 2 關講溢傷（那時他剛開始感覺到天雷符打小妖很浪費）、
第 4 關講符籙譜（那時剛好解鎖第一張新符）、第 10 關講陣法（那時場上格子才夠排）。

三個設計上的硬性約束，都是為了不讓它退化回原本那個東西：
- **兩行是上限**，寫在資料驗證裡，超過就在載入階段報錯
- **一場只上一課**，照順序補。玩家可能一口氣連過好幾關，
  若「所有到期的課」一次倒出來，就又變成一堵沒人看的牆
- **和既有的觸發式提示互相認得**。觸發式的在「事情發生的當下」講，時機更好但不保證會發生；
  課程綁在關卡上保證講得到。哪一邊先講到，另一邊就閉嘴（`LessonDef.hint`）

說明頁沒有刪，降級成查閱用。**教學和參考資料是兩種東西，不該用同一份文件兼任**——
兼任的結果是兩邊都做不好：對新手太長，對老手又不夠精確。

### L-22 [2026-08-29] 持續生效的狀態，要有持續存在的顯示

玩家實測回饋：「不同屬性連成一線的效果**一閃即逝我沒看清楚**，只知道湊不同的圖案成一條線應該會有 buff。」

三件事同時錯了，而且都不是「字太小」這種表面問題：

1. **效果只在成立那一瞬間說一次。** 陣法是持續生效的狀態，回饋卻是一次性的飄字，
   而且 700ms 就淡完——一句十來個字的中文根本讀不完。
2. **兩種陣式畫成同一個顏色。** 同心與五行給的量不同，線卻都是金色，
   所以玩家只學會「湊不同的圖案」那一條路，完全不知道同色也成陣。
3. **加成是逐格不同的**（四角與正中吃三條線、邊中點只吃兩條），
   但畫面上只有「成陣了」這一個布林值，看不出哪一格拿得多。

修法對應三條：線依陣式上色（同心金／五行綠）、每一格右上角常駐標出自己的倍率（`×1.70`）、
HUD 常駐一行「陣法 五行 3 條　+18%」；飄字加 1.1 秒的停留，
而且一次成好幾條時只報一則總結——三行字同時疊上去等於都沒看到。

教訓：**一次性的提示教得會「剛剛發生了什麼」，教不會「現在是什麼狀態」。**
凡是持續生效的機制，畫面上就要有一個持續存在的東西對應它，
否則玩家只能從結果反推規則，而他反推出來的通常只有一半。

### L-23 [2026-08-29] 命中判定取「第一個符合的」，會產生查不出來的誤操作

同一份回饋：「合成常常因為拖曳不準而**變成取代到別的地方的卡片**。」

查下去是硬 bug：陣位的列距是 96px，而落點判定寫成「軸向容差 ±60px、依索引先到先得」。
相鄰兩列的判定區重疊 24px，落在重疊帶時**永遠判給索引較小的那一格**（也就是上面那一列）。
玩家沒有拖歪，是判定把他的手指挪走了。

兩層修法：
- **取最近的一格，不是取第一個符合的。** 用距離排序，重疊就不再有偏袒。
- **距離相當時優先選合得起來的那一格**（26px 之內）。合成是核心動作，
  而且和交換的代價不對稱：交換可以再拖回來，合成錯過的那一張已經被覆蓋掉了。

另外補上**放手前的預覽**：拖曳中即時標出會落在哪一格、以及會發生「合成／交換／放置」哪一件。
三件事在規則上是同一個手勢（`dropOn` 一條路徑），但對玩家來說結果差很多，
差別必須在放手之前就看得到。

教訓：**「第一個符合」是一種沉默的偏袒。** 只要判定區可能重疊，
就得寫成「最近的」；而在動作的代價不對稱時，還要主動偏向代價高的那一邊。

### L-21 [2026-08-29] 篩選門檻不在鑑別區間裡，「前 N 名」拿到的是列舉順序

要從 C(20,4)=4845 種符籙組合裡找最優解，我把篩選跑了**四次**才拿到有意義的排名。

| 次 | 篩選點 | 結果 | 病因 |
|---|---|---|---|
| 1 | 第 30/55/78 關、中期升級 | 通過 0 組 | 升級等級我用猜的（8 級），實際是 15–42 級 |
| 2 | 第 30/55/78 關、實測等級 | 通過 4779/4845 | 門檻太鬆，98.6% 並列 |
| 3 | 第 125 關 | 通過 1220 組 | 還是太鬆；「前 30 名」其實是列舉順序 |
| 4 | 第 132 關 | 通過 0 組 | 太嚴，全滅 |

第 2、3 次最危險：它們**產出了看起來像排名的清單**。第三次的前 30 名裡有 28 組並列，
而先前明明測過能過 130 關的牌組根本沒進榜——因為 `sort` 對等值不改變順序，
取 `slice(0, 30)` 就是取列舉順序。**我差一點把它當成結果送出去。**

還有第五個坑：難度是鋸齒的（見 L-19），所以用**單一關卡**當尺，
量到的是鋸齒相位而不是牌組強弱——第 130 關比第 126 關好過。

最後可用的設計：
- 尺是「一整個三關段的平均勝率」，跨完一個鋸齒週期
- 篩選段先**抽樣校準**（243 組樣本），挑通過率落在 20–60% 的那一段（實測是 121–123 段：53%）
- 冠軍再用 20 顆種子 × 3 關 = 60 場複驗

一個很便宜的自檢：**先看基準組在篩選點的勝率**。落在 0% 或 100%，就是選錯關卡了，
不用等跑完十五分鐘才發現。這一條我前三次都沒做。

### L-20 [2026-08-29] 傳一個沒人接的參數，程式不會報錯，但結論會全錯

修 L-19 的過程中，我用一支探索腳本掃了幾十組數值，每次都寫成
`runOnce(stage, upgrades, seed, pool, 1000)`，標題也寫著「真人 1000ms 操作速度」。
那個 `1000` **從頭到尾沒有被讀取**——那支 `runOnce` 只宣告四個參數，決策間隔寫死 250ms。
JS 對多餘引數靜默忽略，而探索腳本走 vite-node、不做型別檢查，所以一路無聲。

代價：同一關（第 140 關、起手四符）在 250ms 是 **100%**、在 1000ms 是 **0%**。
基於錯誤數據我做了兩件事：把 `ascendStagesPerTier` 選成 2.5（實際該是 2.3），
以及**產出一張標著「真人 1000ms」的最優牌組圖交給製作人，那個標籤是假的**。

是平衡測試把它抓出來的——它自己那支 `runOnce` 有正確吃 `decisionMs`，
於是同一個關卡兩邊給出相反的答案。我一開始還去懷疑 AI 的合成優先序寫錯（那確實也錯了，
順手修掉），繞了一圈才發現是參數根本沒接上。

三條規則：
- **探索腳本和正式測試共用同一份函式簽章。** 兩份各寫各的，就會出現「一邊吃參數、一邊不吃」，
  而不吃的那一邊永遠不會抱怨。
- **新增參數時，先寫一條會因它而改變結果的斷言。** 這次補的第一行就是
  「同一關在 250 / 1000 / 1500ms 要給出不同數字」——三個數字一樣就代表沒接上。
- **兩份工具對同一個輸入給出相反答案時，先懷疑工具，不要先調數值。**
  我差一點就把 2.5 當成結論送出去。

### L-19 [2026-08-29] 兩條指數的斜率不同，走得夠遠一定會分開

製作人問第 130 關那道牆。查下去不是某個數值填錯，是**結構性的**：

| | 每 3 關 |
|---|---|
| 玩家傷害上限 | ×1.35（階數上限 +1） |
| 妖魔血量 | ×1.513（1.148³） |
| 淨值 | **×0.892** |

每三關玩家的相對戰力掉 10.8%，複利下去必然撞牆——真人速度下大約在第 110 關。
主線 81 關看不出來，因為指數要走夠遠才會分開；而**飛升境（82+）從來沒被模擬過**。

順帶找出鋸齒：階上限每 3 關才 +1、血量每關都漲，所以段內難度差 51%。
第 130 關比第 126 關好過（剛跳階），第 96 關比第 97 關難。
這也解釋了搜尋時「用單一關卡當尺會量到鋸齒相位而不是牌組強弱」。

修法：飛升境改用 2.3 關 +1 階（平衡點是 2.174，取 2.3 保留一點下坡），
只在 82 關之後生效，且取新舊規則的較大值——主線一格不動，也不會有任何一關倒退。

三個判準，缺一不可：
- **淨值必須 < 1**，否則進飛升境時多輕鬆就永遠多輕鬆，「無限」變成無限的無聊。
  我第一版取 2.2（淨值 0.9965）就是這樣，第 180 關還是 100%。
- **但要慢到讓飛升境的長度和主線相當**（相對戰力減半約 90 關）。
- **不能動到 1–81**：那條曲線是校準過的，改一個全域參數會把它一起帶走。
  所以新節奏綁在境界上，而不是改 `stagesPerTier` 或 `hpGrowth`。

### L-18 [2026-08-29] 模擬的 AI 有手速，真人沒有——「來不來得及」是它量不到的一整類問題

製作人在第 97 關回報「出怪速度太快，符陣來不及排好就被怪淹沒」。
我第一時間跑模擬，第 97 關 **100% 勝、零損傷**。看起來沒問題——但問題是真的。

差別在 AI 每 250ms 完成一次拖曳、零操作成本，一場做將近**兩百個動作**。真人做不到四分之一。
把決策間隔改成真人的速度重跑，第 97 關：

| 玩家 | 勝率 | 平均成陣條數 |
|---|---|---|
| 模擬 AI（250ms，190 動） | 100% | 5.60 |
| 快手真人（600ms） | 100% | 1.74 |
| 一般真人（1000ms） | 88% | 1.01 |
| **肯花時間排陣（1500ms）** | **25%** | 0.70 |

**排得越慢死得越快。** 陣法是我上一輪剛做成核心的機制，而它在後期不只失效，是變成懲罰。

兩個結構性原因，都不是數值問題：

1. **反應窗口單調縮短，但排一次陣的操作次數固定。** 妖魔走完全程從第 10 關的 12.3 秒
   壓到第 97 關的 6.6 秒；而合成——唯一的指數成長——跟排陣搶同一雙手。
2. **飛升境（82 關以後）從來沒被模擬過。** `STAGES = 81`，無限模式一次都沒跑過。
   而那裡正是速度還在長、玩家早已滿級的地方。

修法（製作人選擇保留手動排陣當技術門檻，不加「一鍵佈陣」）：
給節奏設下限——速度封頂 105→74、出場間隔下限 260→700ms。
第 97 關「肯花時間排陣」的玩家 25% → 88%，而第 1–81 關的所有指標
（零升級撞牆第 9 關、四門派 81 場推完、掉耐久 12/4/6/6%、首領摸門 10/1/5/5）**一個數字都沒動**。

真正的教訓在測試上，有三層：

- **模擬的決策速率是一個假設，不是事實。** 它對「數值夠不夠」合理，對「來不來得及」是嚴重高估。
  凡是牽涉操作預算的規則，要用真人速度另外驗。
- **測試的 AI 不做某件事，就守不住那件事。** 我第一版把真人速度測試接在既有 AI 上，
  結果把速度改回舊值它照樣通過——因為那支 AI 根本不排陣，量不到會排陣的玩家受的傷。
  得另外寫一支會排陣的 AI 才守得住。
- **拿設定值當預期值的測試等於沒測。** 我一度寫成
  `expect(gap).toBeGreaterThanOrEqual(BALANCE.wave.minSpawnGapMs)`——它永遠通過。
  門檻要寫絕對值，而且要**先把數值改回舊的、確認測試真的會紅**再收工。
  這次四條新測試裡有兩條是這樣被抓出來重寫的。

### L-16 [2026-08-29] 加內容之前，先問「它會不會把既有的規則推翻」

製作人要「二十種符籙，每次只能帶四種」。表面上是加內容，實際上它同時碰到三條既有規則：

1. **合成**要湊同種同階。抽符池若含二十種，湊到第二張的機率剩二十分之一，
   遊戲唯一的指數成長來源會停擺。
2. **陣法**的天花板（八條、484 種解）是在「場上只有四種符」的前提下窮舉出來的。
3. **五行陣**要湊三種不同，池子太小又排不出來。

三條一起看，「帶四張」不是我挑的數字，是**三條規則交會出來的唯一解**——
帶五張陣法的推導要重算，帶三張排不出五行陣。
若當初直接做成「二十種全部會出現」，前兩條會同時壞掉，而且壞得很安靜：
玩家只會覺得「後期怎麼合不上去」，不會知道原因。

具體做了三件事把這個前提釘住：
- `TALISMAN_SLOTS` 是常數，陣法的窮舉測試改成對它取冪，改動這個數字測試就會失敗
- `drawCard(loadout, …)` 從 loadout 的四張抽，不再從全域 CARDS 抽——池子的大小變成型別上的事實
- 五副「各走極端」的配置（含一副幾乎不輸出的全輔助）都必須推得完 81 關

教訓：**新功能要先攤開它會碰到哪些既有規則，再決定形狀。**
先做完再回頭補平衡，通常補不回來——因為那時形狀已經固定了。

### L-17 [2026-08-29] 特效不能各寫各的，否則模擬與實戰立刻走散

二十張符有十九種特效（減速、灼燒、斬殺、溢傷轉移、暴擊、光環……）。
最直覺的寫法是每張符一段自己的邏輯，但那樣平衡模擬就得複製一份規則，
而模擬與實戰一旦是兩份實作，模擬出來的難度就不是玩家遇到的難度。

改成**一組固定欄位 + 預設值**：`CardEffect` 有十九個欄位，每張符只填用得到的一兩個，
其餘留在「等於沒有」的預設（倍率類預設 1、加成類預設 0）。
`tickCombat` 一次結算完全部，場景與模擬跑的仍然是同一支函式。

兩個配套的小決定，都是為了讓錯誤現形而不是靜默失效：
- 資料檔裡打錯特效名稱（`slowPercnet`）會**當場報錯**，不會默默不生效——
  那種 bug 只有靠人玩才看得出來
- `cardDps` **刻意不把特效算進去**。溢傷、減速、對首領加成的價值全看戰場長什麼樣，
  硬折成一個數字只會騙人；特效在選符畫面用文字列出，讓玩家自己判斷。
  副作用是模擬的 AI 完全看不懂特效——但那讓模擬對真人一律是**保守**的，這正是想要的方向

### L-15 [2026-08-29] 二選一的規則，代價要由「量」承擔，不是由「有或沒有」承擔

陣法的條件在三個版本裡翻了兩次：
只認同種 → 全同色是純上位解（L-13）→ 改成只認不同種 → 製作人回報「後期通關難度太高」。

兩次都錯在同一個地方：**我用「成不成立」這個開關去做平衡。**
開關只有兩格，調的時候一定會從一個極端跳到另一個極端——
第一版同色拿滿八條，第二版同色拿零條，中間沒有東西。

第三版把同種與不同種**都算成陣，但給不同的量**（同心 14/14/18%，五行 18/18/26%）。
窮舉 4⁹ 重算的結果：八條變得湊得到（484 種解，0.18%），但三種滿陣強弱分明——
全同色 +41%、橫同色縱異色 +52%、拉丁方 +54%。
**同色仍是滿陣裡最弱的一種：它保底，不是最優解。**
取捨還在（照樣要想怎麼擺），但走錯路的人不會被歸零。

模擬驗證：四個門派都是 81 關零重打推完，掉耐久的關卡仍有 4–12%，
首領照樣會摸到山門——後期鬆了，但沒有鬆到沒有壓力。零升級的牆維持在第 9 關。

教訓：**一個機制若有兩種自然的玩法，不要用規則砍掉一種。**
兩種都給、但給不同的量，才調得動；而「量」是連續的，可以慢慢逼近，
「有或沒有」只能在兩個極端之間跳。

### L-12 [2026-08-29] 「清空場上」不等於「贏了」——勝利條件要寫成規則，不能靠副作用

製作人說「最後 boss 沒打死就算沒通關」。查下去發現那不是提議，是回報 bug：
通關條件寫的是「出怪佇列空了而且場上沒有敵人」，而首領走到山門時會被當成漏怪**移除**——
於是首領大搖大擺走進山門，場上清空，判定通關。模擬量出來：**81 關裡有 10–20 關是這樣過的。**

更糟的是難度校準是建立在這批假通關上的。修掉之後零升級的牆從第 20 關掉到第 4 關，
整條曲線得重調（最後定在 boss 血量 3000、速度 30、砸門每 1.8 秒扣三倍，零升級牆回到第 9 關）。

修法不是加一個「首領死了沒」的判斷式，而是把規則寫進結構：
**首領走到山門不會離開，牠停在門口一直砸，直到被斬掉或山門破。**
首領永遠在場上，「場上清空」就自然蘊含「首領已死」。
（`bossKilled` 旗標仍然明寫在通關條件裡——那是規則，不是可以靠副作用推導的東西。）

教訓有兩層：
- 勝負條件是規則，要正面寫出來。用「場上沒東西了」這種間接條件去推導，
  任何一條「把單位從場上移除」的路徑都會變成漏洞。
- **一個影響勝負的 bug 會污染所有以勝率為基礎的數值校準。** 修 bug 之後，
  之前所有「已經調好」的數字都要重新驗一次。

### L-14 [2026-08-29] 機制的天花板要算出來，不是估出來

改成「不同種才成陣」之後，製作人問：那還湊得到八條連線嗎？
這種問題可以用直覺回答（「應該很難吧」），但直覺沒有用——**窮舉 4⁹ = 262144 種擺法就有答案**。

答案是七條，八條在數學上不可能：四個角落被六條線兩兩約束成互不相同，
因此四個角就用掉全部四種符；而中央同時在兩條斜線上，必須與四個角都不同，
沒有第五種符可用。七條有 1056 種解（0.4%），全場平均加成 +49%。

兩個收穫：
- 這個上限來自「符種只有四種」。**日後若新增第五種符，八條就變成可能，天花板會整個跳起來。**
  因此把窮舉寫成測試釘住，並先斷言符種數是 4——加第五種時那條測試會失敗並要求重算。
- 設計一個會互相疊加的機制時，「最滿是多少」應該是**算出來的數字**而不是估計值。
  上一輪就是因為沒算，才讓全同色的 +81% 上線。

**後續（L-15）**：規則再改成「同種與不同種都成陣」之後，這個天花板重算過——
四種符也湊得到八條（484 種解，0.18%）。絆線測試照樣有用，它在規則變的那一刻就失敗了，
逼我重跑窮舉而不是沿用舊結論。

### L-13 [2026-08-29] 加成會疊加時，要算的是「疊到最滿」的那一格

陣法上線當天製作人就問：「全部同色的話加成是不是過高，變成每場只要湊九張同色？」
算出來正是如此：3×3 全同色會同時成立三橫、三縱、兩斜共八條陣，
每一格至少吃到橫＋縱，中央還吃到兩條斜線——而同種本來就最好合成。
**湊同色是純上位解，我設計時想像的取捨根本不存在。**

我原本的測試只驗了「每一條陣的加成小於一階成長」，卻沒驗**疊加後的單格上限**。
單項合理不代表總和合理，這是我漏掉的那一格。

修法是把條件反過來：**一整條線上每一張符都要不同種才成陣**。
於是「合成要湊同種、結陣要湊不同種」直接對立，全同色的陣法數是零，
取捨從規則裡長出來而不是靠數字壓制。順帶把兩條斜線對中央格的加成去重，
讓單格上限是可預期的（現在小於兩階的成長，並有測試把關）。

### L-11 [2026-08-29] 畫了格子就要給格子意義，否則畫面在說謊

製作人問「符籙水平或垂直還要斜角連線有額外加成嗎」。答案是沒有——
場上是一個 3×N 的格子，但擺哪一格對輸出完全沒有差別，六個陣位彼此可以任意互換。

這是我的疏漏，而且是**畫面先承諾了、規則沒兌現**的那一種：
玩家看到網格就會假設位置有意義，花時間去排，然後什麼也沒發生。
一個不影響任何結果的介面元素，比沒有這個元素更糟。

補上陣法之後，兩個設計決定值得記：

- **條件是「同種」不是「同階」**。同階的兩張本來就該合成，用同階當條件等於在懲罰合成。
  用同種當條件才會形成真正的取捨：合成會空出一格、把線打斷，
  所以「現在合，還是先讓這一列成陣」變成每隔幾秒要做一次的決定。
- **加成一律小於一階的成長**（`tierGrowth` 1.35）。陣法是「排得好有賞」，
  不能大到讓「為了排陣而不敢合成」變成最佳解——長期唯一跟得上難度的還是合成。
  實測六格的場上，同一組牌按種類排好比交錯排多約 26% 輸出，量級剛好。

斜陣只在 3×3（陣法擴充買滿）成立，順手把原本最弱的那條升級線變成有獨佔獎勵的一條。

量測時踩了一個坑值得一併記：我原本想量「排得好 vs 排得差」的差距，
結果九格那組的「差排」意外排出了三條縱陣，量出來反而是 −2%。
**要量一個機制的上限，得先確認對照組真的沒有觸發那個機制。**

### L-10 [2026-08-29] 教學不能靠運氣：腳本化的一步要有腳本化的牌

寫新手教學時第二步是「把同種同階的兩張符疊起來」。照正常流程開局，起手牌是隨機抽的——
三張符有可能兩兩都不同種或不同階，那第二步就永遠完成不了，新玩家會直接卡死在教學裡。

所以教學用**固定起手牌**：場上全空、手上三張同種同階。三張一樣是刻意的，
不管玩家先放哪一張，剩下的都一定合得起來。這個覆寫只發生在呈現層（RunScene），
`createDefenseState` 與平衡模擬完全不知道有教學這回事。

另外兩個決定也記一下：

- **教學的前兩步把戰鬥停住**。新手還在找哪裡可以拖的時候，妖魔不該已經走到山門——
  第一關的第一印象是「我搞懂了」，不是「我還沒搞懂就輸了」。
- **做到才往下走，不用計時器**。摸索久一點不會被推著跑，做完也不必等動畫跑完才能操作。

「看過教學沒有」存在 `player.hints`，v4→v5 遷移把 `runs > 0` 的舊存檔直接標成看過——
已經玩了幾十場的人不該被抓回去上課。

### L-09 [2026-08-29] 測試用了 Math.random 就不是測試，是抽獎

版面修好之後 CI 掛了，掛的是一支跟這次改動完全無關的測試：
「撥弦的基頻與指定音高相符」在 660Hz 量到 1423Hz。本機跑三次都過，前一次 CI 也過。

原因是 `renderPluck` 的起音是 `Math.random()` 的噪音脈衝，每次波形都不同——
這支測試從寫下來的第一天就是機率題，只是一直沒抽到壞籤。

**「偶發」不是結論，是還沒找到原因。** 這次拆成兩層各自修：

1. **測試端**：以固定種子的亂數取代 `Math.random`，一次驗五個種子 × 三個音高。
   可重現，而且不會因為「剛好挑到好種子」而放過真的問題。
2. **偵測端**：自相關改為依重疊長度正規化、且只接受「區域極大值」。
   以 300 種子 × 6 音高共 1800 次量測掃描接受門檻：0.9 失敗 3 次、0.95 失敗 1 次、
   0.97 起為 0 次，因此定在 0.97。

過程中還發現一件真的音訊問題，不只是測試的問題：純白噪音起音有時高頻能量遠大於基頻，
那一下撥弦聽起來會**高八度**。`renderPluck` 因此在噪音進迴路前先去直流、再沿環平滑一次，
八度誤判率由 0.8% 降到 0。**把偶發測試追到底，追出來的是產品缺陷。**

### L-08 [2026-08-29] 版面是資料的函式，改資料筆數就要重算版面

升級線從五條變六條之後，洞府整頁溢出：六列（每列 122＋間距 10）疊到 928，
把摘要（832）與按鈕（900）整個蓋掉。**製作人是在手機上看到才回報的**，
而我在改玩法那一輪只截了關卡畫面，沒有回頭看洞府。

問題不在「忘記調座標」，而在版面座標把「有幾筆資料」當成了常數。
凡是「依 data/*.json 的筆數排出來的畫面」都有同一個風險，這次一起檢查了三處：

| 畫面 | 依賴的資料 | 症狀 |
|---|---|---|
| 洞府 | `upgrades.json` 筆數 | 五 → 六條後蓋住摘要與按鈕 |
| 拜入師門 | `sects.json` 筆數 | 第四張卡底緣被按鈕切掉 13px |
| 結算 | 單次解鎖的成就數 | 一次解鎖十條時往下疊滿整頁，蓋住結算表 |

三處的修法不同，但原則一樣——**讓版面對筆數不敏感**：
前兩者把列高與起點重算成「筆數 × 列高 ≤ 可用高度」並在註解裡寫出這條算式，
第三者則改成**一次只顯示一條、逐條輪播**（帶 n/N 序號），這樣不管解鎖幾條都不會溢出。

順帶修掉一個看起來像壞掉的地方：成就只在結算畫面結算，
所以「仙途錄」會出現「進度 7/6 階」卻標成未達成。改為進入仙途錄時也一併結算。

### L-07 [2026-08-28] 換玩法時，先重寫模擬再寫畫面

這次把整個玩法從「閘門跑酷」換成「法寶符卡牌塔防」。做對的一件事是**順序**：
先寫純函式的 `defense.ts` 與平衡模擬，把數值跑到收斂，才動 `RunScene`。
場景後來一次寫成就能用，因為所有規則在寫畫面之前就已經定案且驗證過。

架構上刻意讓**場景與模擬呼叫同一支 `tickCombat()`**。上一版的模擬是另寫一套近似模型，
「模擬過的數值」與「玩家實際遇到的數值」永遠有落差；共用同一支之後這個落差消失了。

調參過程中，四次失敗都不是「數字沒調好」而是**結構錯了**，值得記下來：

1. 階數上限每個境界 +1 → 同一境界內難度漲 2.4 倍而玩家完全不成長，
   末尾打不動、一突破就輾壓。週期縮短到三關就平順了。
2. 合成只能發生在場上 → 抽到和場上對不上的符變成純損失，模擬中 AI 一直在棄牌。
   開放手牌之間也能合，決策層才完整。
3. 漏怪代價固定 → 「聚眾成軍」一條線就能把邊界關卡推後 16 關，其他五條加起來還不如它。
   代價隨關卡成長之後才回到「多一點容錯」的定位。
4. 「對首領傷害」在這個類型裡是死線 → 量測顯示邊界關卡幾乎都輸在雜兵漏怪，
   買到 +80% 也推不動任何一關。整條砍掉換成「陣法擴充（多一個陣位）」。

第 4 點也讓平衡測試改了量法：不再問「買了這條線邊界關卡有沒有往後移一整關」，
而是問「在快打不過的那一關，買了它勝率有沒有上升」——後者才是玩家在乎的事，
而且不會被「一整關」的粒度掩蓋掉真實效果。

### L-06 [2026-08-28] 參考別的遊戲要拆成機制，不是照抄畫面

製作人給的參考影片是一款骰子卡牌塔防，和本作的閘門跑酷是不同類型，照抄等於重做。
拆開來看，影片真正好用的是三件「和類型無關」的事：命中處會跳傷害數字、
角色在接觸前就會自動輸出、以及一個會越疊越高的倍率牌。這三件都能長在既有玩法上。

借進來的時候必須各自綁一條真的規則，否則只是特效：
齊射綁「射界寬度有限」所以走位有回報、連擊綁「只放大金幣與開場氣勢」所以不會抹平本關難度。
沒有規則的借用會變成畫面很熱鬧但操作沒有變化。

### L-01 [2026-08-27] 數值平衡不能用紙上推估，要用模擬跑

現象：第一版數值在瀏覽器實測時，第 1 關就被首領打光；把公式從紙上估的「血量 ÷ 每秒傷害 = 戰鬥秒數」
推導出來的數字填進去，實際跑起來完全不是那回事。

原因：戰鬥是有回饋迴路的系統——首領打死人 → 我方輸出下降 → 更打不死首領。
靜態的除法算不出這種雪崩，只能跑模擬。另外「玩家每關會拿多少金幣、能買幾級升級」也是一條
長期曲線，單看一關的數字看不出中後期會不會脫節。

機制化措施：`tests/balance.test.ts` 用程式把完整循環（挑戰 → 拿金幣 → 升級 → 下一關）跑完 30 關，
四個門派各跑一遍，並斷言：沒有卡關、平均嘗試次數、首領戰長度分布、零升級會在中後期撞牆。
**改 `data/*.json` 的數值之後這組測試會直接反映後果，CI 也會擋。**

### L-05 [2026-08-27] 加算的成長永遠追不上指數的難度

現象：五條升級線裡有四條買了完全不影響能推到第幾關，玩家買升級毫無感覺。

原因：關卡難度與閘門收益都是指數成長，而四條線是固定加值。
固定加值在後期會被稀釋到看不見——這不是數值大小的問題，是**曲線形狀**的問題。
唯一有效的第五條線，正好是原本就採乘算的那條。

機制化措施：`tests/balance.test.ts` 之外，量測手法改用「邊界關卡移動幾關」而非勝率
（勝率在 90% 以上會飽和，看不出差異）。設計上的規則是：
**任何長期成長的獎勵，其曲線形狀必須與難度曲線相同**，否則遲早失效。
金幣收入同理，已一併改為等比成長。

### L-04 [2026-08-27] Pages 部署失敗要看 deploy job 有沒有步驟

現象：`build` 全綠但 `deploy` 在數秒內失敗，連續三次；期間誤判過兩次原因
（先猜「Pages 沒啟用」，再猜「預設分支不是 main」），都改了設定卻仍然失敗。

原因：`deploy` job 失敗時**步驟數為 0**，代表它根本沒開始執行，是被 `github-pages`
環境的部署分支規則擋下——該規則存的是環境建立當時的分支名（`claude/game-design-beg7xo`），
改預設分支不會改寫這條既存規則。

機制化措施：往後遇到部署失敗，先看 `deploy` job 的步驟數：
- 步驟數 0 → 環境／權限問題，去 Settings → Environments → github-pages 看部署分支規則
- 有步驟且某步失敗 → 才是產物或設定的問題
另外，改完設定要用**全新的一次執行**驗證（`workflow_dispatch`），
re-run 舊執行沿用舊 context，會讓人分不清是設定沒生效還是別的原因。

### L-03 [2026-08-27] 「不能產生圖像」不等於「不能做美術」

現象：一度把美術整項掛在「需要外部素材」上，交出只有幾何圖形的畫面。

原因：把「無法生成點陣圖」誤當成「無法產出美術資源」。手寫 SVG、程式合成音訊都是
可以直接交付的做法，只是風格會被限制在單色剪影與簡樸音色。

機制化措施：資源類需求先問「有沒有以程式碼為載體的形式」，
再決定要不要標成外部依賴。目前 `public/art/` 與 `src/audio/` 就是這條路的產物。

### L-02 [2026-08-27] Phaser 的 word wrap 不斷中文

現象：門派卡的說明文字整段溢出面板外。

原因：Phaser 的 `setWordWrapWidth` 只在空白字元處斷行，中文沒有空白就整段不斷。

機制化措施：`src/ui/theme.ts` 的 `wrapText()` 自行估算字寬斷行（全形 1 em、半形 0.55 em），
所有多行文字一律走它；`tests/ui.test.ts` 有對應測試。

---

## 六、已完成紀錄

### 玩法轉型：閘門跑酷 → 法寶符卡牌塔防（2026-08-28）

製作人指定「改成影片那種遊戲」。參考影片是骰子卡牌塔防，與原本的閘門跑酷是不同類型，
因此不是加功能而是換玩法。以下是這次的完整範圍。

**做法上的順序**：先寫純函式的 `defense.ts` + 平衡模擬，把數值跑到收斂，才動 `RunScene`。
場景後來一次寫成就能用。架構上讓場景與模擬**呼叫同一支 `tickCombat()`**，
消除了「模擬過的數值」與「畫面實際行為」之間的落差（見 L-07）。

#### 新增

- `data/cards.json`：四種法寶符（劍陣／天雷／風刃／烈焰），差異在傷害、出手間隔、目標數
- `src/systems/deck.ts`：階數、傷害公式、合成規則、階數上限隨關卡成長
- `src/systems/defense.ts`：出怪排程、開火、漏怪、勝負判定；`tickCombat()` 是唯一的推進入口
- `src/ui/card.ts`：符牌外觀，手牌與陣位共用
- `public/art/glyph-{sword,bolt,fan,flame}.svg`：四種符的圖騰
- `tests/defense.test.ts`（20 項）：合成規則、門派被動、溢傷、掉幀不影響輸出總量

#### 移除

- `src/systems/run.ts`、`src/input/`、`data/gates.json`、`public/art/gate-arch.svg`
- `tests/run.test.ts`、`tests/input.test.ts`

#### 改寫

- `balance.json` 全部重寫；`sects.json` 四個被動全換；`upgrades.json` 六條線
- `RunScene` 重寫為防守戰場景；`ResultScene`／`SectScene`／`UpgradeScene` 的欄位與文案跟著換
- 存檔升到 v4（統計欄位換成新玩法的原始事實），v3→v4 遷移保留金幣、關卡、升級等級與成就

#### 數值調校（模擬跑出來的，不是憑感覺）

四次失敗都不是「數字沒調好」而是結構錯了，四項都已修正並記在 L-07：
階數上限的週期、合成的可用範圍、漏怪代價的成長、以及「對首領傷害」在這個類型裡是死線。

最終狀態：四個門派都能一路推到第 81 關；零升級在第 20 關上下撞牆；
全程約兩成的關卡會漏怪；六條升級線在「快打不過的那一關」都能明顯提高勝率。

驗證：`tsc --noEmit` 通過、83 項測試通過；瀏覽器實測完整流程（含 360×640）無錯誤，
並逐一確認拖放的三種結果、合成演出、波次推進、首領血條與失守結算。

### 四角度審視與修正（2026-08-27）

以資深玩家／UI 設計師／遊戲設計師／美術設計師四個角度重新檢視，
量測後修正的項目如下。**每一項都以模擬器或瀏覽器實測驗證過，不是憑感覺調整。**

#### 一、升級系統形同虛設（遊戲設計，最嚴重）

量測方法：對每條升級線多買三級，看「勝率跌破 50% 的第一關」（邊界關卡）移動幾關。

原始結果：

| 升級線 | 邊界移動 |
|---|---|
| 招收弟子（加算） | +0 關 |
| 護體罡氣（加算） | +0 關 |
| 淬鍊功法（加算） | +0 關 |
| 聚寶之術（乘算） | +0 關 |
| 斬妖訣（**乘算**） | **+15 關** |

五條線裡唯一有效的是唯一的乘算線。原因：閘門的加算值隨關卡等比放大，
固定加值（起始人數 +1）在後期被稀釋到看不見。**加算永遠追不上指數。**

修正：
1. 五條線全部改為百分比乘算，並改成作用在不會被稀釋的地方——
   聚眾成軍乘在「起始人數與所有人數閘門收益」、護體罡氣乘在減傷、
   淬鍊功法乘在單兵戰力、聚寶之術與斬妖訣維持原本的乘區。
2. 金幣獎勵由線性改為等比成長。升級花費是指數曲線，收入若是線性，
   深層等級永遠買不起，中後期的升級系統等於停擺。
3. 難度重新校準（`hpBase` 2100→6500、`hpGrowth` 1.28→1.29）。

修正後：零升級的邊界由第 43 關降到 **第 15 關**（升級成為必需），
早期每三級可推進 8–9 關，81 關完整循環模擬平均 1.17 場過一關。

#### 二、玩家看不到自己的核心數值（玩家＋UI）

戰力是決定一切的綜合數字，但畫面上從來沒出現過；玩家只看得到弟子與武裝兩個
分項，無從判斷閘門該選哪邊。HUD 重整為：境界／金幣 → **戰力（最大字級）** →
弟子・武裝・路程 → 路程進度條。

順帶解決長期存在的文字重疊：浮在隊伍頭上的人數字與閘門文字會互相蓋住，
既然 HUD 已有弟子數，直接移除浮動文字，這類重疊一次消失。

#### 三、失敗學不到東西、關卡無法中離（玩家）

- 失敗只顯示「道消」。改為依當場的武裝佔比與失敗原因給**具體診斷**
  （例：「武裝值太低，敵陣的比例傷亡吃掉了整支隊伍——多走武裝閘門，或提升護體罡氣」）。
- 關卡進行中新增「放棄」按鈕，結算為失敗但不推進關卡。

#### 四、隊伍在跑但畫面靜止（美術）

- 路面加上等距橫紋，跟著關卡速度往下捲，速度感立刻出現。
- 通過閘門與衝散敵陣時炸開光點粒子（貼圖以程式產生，不需素材檔）。
- 洞府顯示五個乘區的實際倍率，讓每次購買看得到結果。

#### 四項延後項目（已於同日補完，見下一節）

### 補完四項延後項目（2026-08-27）

1. **門派被動，改變決策而非只有數值**
   - 體修「銅皮鐵骨」：每關前兩次敵陣完全免傷 → 可以先放心堆人數
   - 劍修「一劍破萬法」：首領戰開場氣勢全滿但衰退加倍 → 必須速戰速決
   - 符修「符籙鎮邪」：陷阱閘門完全無效 → 可以無視陷阱側，選擇邏輯不同
   - 丹修「丹藥回春」：金幣閘門回復一成門人 → 拿錢與保人不再互斥
   全部由 `sects.json` 驅動，門派卡上直接標示。

2. **首領戰的戰術取捨**
   手指停住不動超過 `guardIdleMs` 即轉為守勢：**輸出砍半，挨打也砍半**。
   隊伍快被磨光時停手保命、血量健康時猛攻速殺。
   沿用既有的拖曳輸入，不新增按鈕。

3. **成就系統（仙途錄）**
   16 條長期目標，條件寫在 `achievements.json`；存檔升至 v3（新增 `achievements`
   與長期統計 `stats`）並補上 v2→v3 遷移。結算時浮出達成提示並發金幣，
   標題頁新增「仙途錄」可看全部進度（可拖曳捲動）。

4. **十個境界各有地貌**
   不再只換色：山巒／竹林／雲海／火山／仙宮／浮空島／劫雲／仙庭八種遠景，
   由 `realms.json` 的 `scenery` 指定。
   飛升境的境界色由純白改為冰藍白——純白會讓整個背景泛灰、字讀不到。

### 妖獸分種與門人升級（2026-08-27，製作人指定）

1. **妖獸依名字畫成對應的生物**，不再全部套用同一款：
   妖狼（尖耳長吻蓬尾）、妖熊（圓耳厚身短尾）、雪怪（雙足白毛長臂）、
   蜈蚣（七節多足帶觸鬚）、火蠍（雙螯翹尾毒針）、蟒蛟（S 形環節吐信）。
   `enemies.json` 的敵陣名稱一併調整，讓每個境界遇到的妖獸種類不重複
   （新增玄冰雪怪、噬魂蜈蚣、虛空蟒蛟、太古妖熊，赤炎蠍潮改名赤炎火蠍潮）。
   四種人形敵陣（流寇／屍傀／魔修／天兵）維持不變。
2. **門人造型隨境界分三階**：初階素袍（煉氣–金丹）→ 中階加金邊披肩（元嬰–煉虛）
   → 高階加披風、頭冠與周身靈光（合體以上），門派專屬配件也跟著升級。
   階級由 `discipleTierForRealm()` 依境界索引決定。
3. 產生器 `scripts/gen_art.py` 擴充為 24 張門人（4 門派 ×3 階 ×2 幀）
   ＋ 20 張敵陣（10 種 ×2 幀），共 44 張。

   踩到的細節：高階披風原本畫到腳底，把腿完全蓋住、走路動畫就看不見了，
   下緣改為止於腰帶上方。另外蜈蚣與火蠍的 viewBox 較寬，
   貼圖寬度必須跟著 viewBox 走，否則會被拉扁成正方形。

### 人物全彩化與走路動畫（2026-08-27，製作人指定）

原本所有角色都是純白剪影 + `setTint` 染一個色，畫面只有單色塊；動畫也只有上下浮動。

1. **全彩重繪**：人物改為真正上色（袍色、膚色、髮色、腰帶、兵刃、葫蘆、符紙各有顏色），
   不再用 `setTint`。門派差異來自造型與配色本身。首領一併重新配色，
   境界色只保留在身後的光暈，本體不再整隻染成一個色調。
2. **走路兩幀動畫**：每個角色兩張貼圖組成走路循環（Phaser 的 `anims` 可直接以貼圖 key 列 frames，
   不需要 spritesheet）。群像中每人依自己的相位錯開動畫進度，整團才不會像同一個人複製三十份。
3. **以程式產生美術**：`scripts/gen_art.py` 產生九個角色 ×2 幀 = 18 張 SVG。
   兩幀之間只有四肢差異、其餘完全一致，手寫很難維持這種一致性；改配色也只要動一個色表。
4. **閃白改用 `setTintFill`**：全彩貼圖上 `setTint(0xffffff)` 等於沒上色，命中閃白要用 `setTintFill`。

### 境界擴充、關卡加長與人物造型（2026-08-27，製作人指定）

1. **每個境界九層**：`realms.json` 的區間由每境界 3 關改為 9 關，一輪十個境界共 81 關
   （原本 28 關就進飛升境）。`realmTitle()` 已能顯示到「九層」。
2. **關卡加長**：`gatesPerStageBase` 4→6、`gatesPerStageMax` 9→14，一關約 8–20 個遭遇。
3. **九款人物造型**：門派四款（體修／劍修／符修／丹修）、敵陣五款
   （妖獸／流寇／屍傀／魔修／天兵），分別由 `sects.json` 與 `enemies.json` 的 `art` 欄位指定。
   單位再放大：門人 44→62 px、兵卒 58→76 px；畫面上最多繪製人數 48→30，
   換取每個人的造型看得清楚（實際人數仍以數字顯示）。
4. **數值重新校準**：境界壓制改為每 9 關才進一階，玩家成長變慢，原數值在第 41 關卡死。
   以模擬掃描重新定案 `hpGrowth` 1.295→1.28、`gateValueGrowth` 1.08→1.09，
   四個門派 50 關全通、平均 1.25 場過一關、79% 的首領戰落在 2.5–20 秒。
   平衡測試的涵蓋範圍由 30 關提高到 45 關。

   同時調整「零升級撞牆」的斷言門檻（25→40 關）：關卡總長從 30 關變成 81 關，
   原門檻已不是同一個意思，改以「全程的一半」為準並在測試內註明理由。

### 操作與演出調整（2026-08-27，製作人指定）

1. **操作改為觸控跟隨**：原本是離散左右滑在兩條車道間跳，改成隊伍連續跟著手指走。
   TECH_SPEC 第 4.5 節原本明文「禁止連續拖曳」，這條是舊題材留下的，已依製作人要求改寫並註記變更理由。
   `src/input/swipe.ts` 換成 `src/input/follow.ts`（夾邊界、指數逼近、氣勢累積三個純函式，含測試）。
   逼近改用 `1 - e^(-k·t)`，高低畫格率的手機手感一致。
2. **閘門去底色**：拿掉色塊，只用山門線條與文字的顏色區分資源（弟子綠／武裝藍／金幣金／陷阱紅），
   文字加深色描邊維持可讀性。`gate-arch.svg` 的匾額一併移除，避免文字疊在色塊上。
3. **敵我單位動畫化**：
   - 門人：跑動起伏加輕微擠壓，每人相位不同，整團看起來在趕路
   - 敵陣：改為七名獨立兵卒（新的 `enemy.svg`），原地踏步逼近，被衝散時閃白彈開；
     原本整排剪影的 `mob-line.svg` 已刪除
   - 首領：待機起伏擺動、出手前蓄力再撲下、中招閃白縮身；劍氣改用加亮混色，不再糊在臉上
4. **單位放大**：門人 30→44 px、兵卒 40→58 px，人多時的縮小下限也提高，遠看仍認得出是一個個人。

### 上線與舊題材清理（2026-08-27）

- 併入 `main` 並切為預設分支，GitHub Pages 部署成功
- 刪除舊題材（史萊姆 RPG）的設計規格，`GAME_DESIGN.md` 改為《問道飛升》的規格
- `TECH_SPEC.md` 的目錄結構、資料驅動範例、存檔結構與輸入層規範同步改寫成現況
  （移除探索場景／搖桿、吸收／形變／分裂、體力與離線結算等已不存在的機制）
- 刪除舊分支 `claude/game-design-beg7xo`

### 《問道飛升》首個可玩版本（2026-08-27）

新增檔案：

- `data/`：`balance.json`、`realms.json`、`cards.json`、`sects.json`、`upgrades.json`、`enemies.json`、`achievements.json`
- `src/data/`：`types.ts`（型別）、`validate.ts`（runtime 驗證）、`index.ts`（載入即驗證）
- `src/systems/`：`defense.ts`（出怪／開火／漏怪／勝負的全部運算）、`deck.ts`（法寶符與合成）、
  `loadout.ts`、`realms.ts`、`upgrades.ts`、`achievements.ts`、`rng.ts`
- `src/input/`：已移除。輸入改為拖放，判定寫在 `RunScene` 與 `defense.dropOn()`
- `src/save/`：`types.ts`、`storage.ts`（抽象層）、`migrations.ts`、`index.ts`
- `src/scenes/`：`BootScene`、`TitleScene`、`SectScene`、`RunScene`、`ResultScene`、`UpgradeScene`
- `src/ui/`：`theme.ts`、`button.ts`、`backdrop.ts`
- `public/art/`：九張手寫 SVG；`src/art.ts` 負責載入與尺寸
- `src/audio/`：`notes.ts`（五聲音階）、`synth.ts`（Karplus-Strong／鑼／噪音／悶響）、`index.ts`（播放與配樂排程）
- `tests/`：`config`、`data`、`defense`、`loadout`、`save`、`ui`、`balance`、`audio`

**與規格的差異（需製作人追認）**：

1. TECH_SPEC 第 3 節提案用 zod 做 runtime 驗證，這裡改為手寫驗證器（`src/data/validate.ts`）。
   理由：只有六個資料檔、結構單純，為此多一個 runtime 相依沒有效益；驗證強度維持規格要求
   （格式錯誤在載入階段就 throw，不會等到跑到該筆資料才崩）。不接受可以換回 zod。
2. 關卡場景除了左右滑動，另外接受「點擊畫面左／右半邊」選邊。理由是桌機測試與誤觸補救；
   它仍是單次離散輸入，不是搖桿，不違反第 4.5 節的兩套輸入分離。
3. `src/` 內仍有畫面座標（車道 X、面板大小、字級）與音訊合成參數，未搬進 `data/`。
   第 3 節規範的是「遊戲數值」，這些是版面與音色，搬進 JSON 只會讓調整變麻煩。
   會影響玩法的部分（首領造型對應哪個首領）仍寫在 `enemies.json`。
4. 存檔版本由 v1 升到 v2（新增 `settings.sound`），並依第 4.2 節寫了 v1→v2 遷移，
   舊存檔的進度與金幣不會流失（`tests/save.test.ts` 有對應測試）。

**尚未驗證**：GitHub Pages 線上實機與手機觸控手感。Pages 的 Source 設定需製作人操作。

### 階段 0 — 專案初始化（2026-08-15）

Phaser 3.90.0 / TypeScript 7.0.2 / Vite 8.2.1 / Vitest 4.1.10，精確版本並提交 lockfile。
目錄骨架依 TECH_SPEC 第 2 節建立，`tsconfig.json` 開 `strict` 並加上 `noUncheckedIndexedAccess`、
`exactOptionalPropertyTypes`；`vite.config.ts` 的 `base` 設為 `/SB-Game/`；
`deploy.yml` 走 `npm ci → tsc --noEmit → npm test → build → 部署 Pages`，另加 `workflow_dispatch` 手動觸發。
