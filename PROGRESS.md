# PROGRESS.md — 進度與待辦

> **每次 session 開頭先讀本檔，結束前更新本檔。**
> 這是跨 session 的唯一記憶來源，不依賴對話歷史。

最後更新：2026-08-15（第 5 次修訂：戰鬥改 2.5D、階段 A 改拖曳、閘門推進原型完成）
**現階段範圍：單機版。** 不做後端、帳號、廣告、內購，僅遵守 TECH_SPEC 第 9 節的搬遷預留規範。
目前階段：**階段 1b（階段 A 閘門推進）已完成 → 待決定下一步**

> **執行順序變更**：製作人決定把 1b 提前到 1a 之前，先驗證戰鬥手感再做探索場景。
> 探索場景的規格未因此改變。

---

## 一、階段總覽

| 階段 | 內容 | 狀態 |
|---|---|---|
| 0 | 專案初始化、CI、可部署的空白頁 | **完成**（2026-08-15） |
| 1a | ExploreScene：2D 自由走動、搖桿、碰撞、遭遇觸發 | 未開始 |
| 1b | BattleScene：2.5D 偽透視、拖曳走位、閘門推進（階段 A） | **完成**（2026-08-15） |
| 1c | BattleScene：閃避反擊（階段 B）、兩階段串接 | 未開始 |
| 2 | 吸收系統（上滑確認）+ 能力欄位 | 未開始 |
| 3 | 存檔／讀檔 + 遷移框架 | 未開始 |
| 4 | 形變系統（液化、硬化） | 未開始 |
| 5 | 區域 2、3 與剩餘形變 | 未開始 |
| 6 | 分裂系統與離線結算 | 未開始 |
| 7 | Capacitor 封裝 | 未開始 |

---

## 二、當前待辦

### 部署現況（重要，動 CI 前先讀）

首次部署已成功：<https://bozhan9527-ux.github.io/SB-Game/>

**但目前 push 到 `main` 不會部署成功。** 原因見下方 L-01。
現行可用的部署方式：**在 Actions 頁面對 `deploy.yml` 用 `workflow_dispatch` 手動觸發，且 ref 必須選 `claude/game-design-beg7xo`。**

- [ ] **待辦：把預設分支改為 `main`**（Settings → General → Default branch）。
      2026-08-15 首次嘗試時 GitHub 網頁版回報 `Could not change default branch`，
      兩個分支都存在且無保護規則，疑為手機版網頁的暫時性問題，改用桌機再試。
      改完之後 push 到 `main` 即可自動部署，屆時本節可刪除。

### 需手機實測後定案的數值

以下都已有可運作的預設值，但紙上無法定案，必須在手機上調到手感對為止。
全部位於 `data/balance.json`，調整不需動 `src/`。

| 參數 | 目前值 | 說明 |
|---|---|---|
| `gateRun.speedZPerSecond` | 3.2 | 推進速度。決定閘門間隔的節奏感 |
| `gateRun.gateSpacingZ` | 4.5 | 閘門間距。目前約每 1.4 秒一道，可能偏快 |
| `gateRun.deadZoneX` | 0.12 | 中線死區寬度。太寬會誤判撞牆，太窄則失去「必須主動選邊」的意義 |
| `drag.pixelsPerUnitX` | 110 | 拖曳靈敏度。手機螢幕寬度與手指行程差異大，必須實測 |
| `render.labelMinScale` | 40 | 閘門標籤從多遠開始顯示。目前約提前 2 秒 |

### 下一步（待製作人選擇）

- **階段 B（滑動閃避）**：接續階段 A，完成一場完整戰鬥
- **階段 1a（ExploreScene）**：回到原訂順序，補上探索與遭遇觸發
- **先調手感**：不加新功能，依上表在手機上把節奏調對

---

## 三、待製作人決策（阻塞項）

以下項目標記為 `[待定]`，實作到相關部分前需先確認。**卡在製作人這邊。**

| 編號 | 項目 | 提案 | 需要時機 |
|---|---|---|---|
| ~~D-01~~ | ~~等級上限~~ | **已決：Lv.200，經驗曲線改為 `30 * n^1.4`** | — |
| ~~D-02~~ | ~~戰鬥視角~~ | **已決（2026-08-15 修訂）：探索 2D 俯視自由走動；戰鬥改為 2.5D 偽透視推進。階段 A 按住拖曳走位，階段 B 離散左右滑閃避** | — |
| ~~D-03~~ | ~~戰鬥失敗懲罰~~ | **已決：扣體力（小怪3／勇者5／魔王10），回存檔點，不掉等級與素材。體力 Lv.1 為 20，每級+10，每 10 分鐘回 1** | — |
| D-09 | 預兆時間窗基礎值 | 0.45 秒 | 階段 1c（需手機實測後定案） |
| ~~D-10~~ | ~~閘門數~~ | **已決：小怪 3-5 隨機，勇者 5-8 隨機，魔王固定 10** | — |
| D-04 | 凝膠值上限 | 300 | 階段 4 |
| D-05 | 分身上限 | 5 隻 | 階段 6 |
| D-06 | 離線結算封頂 | 12 小時 | 階段 6 |
| D-07 | 首章目標時長 | 2-3 小時 | 階段 5 |
| D-08 | 美術來源 | **Claude 無法產生圖像**。可先用程式繪製幾何佔位圖（史萊姆＝圓、勇者＝三角）讓機制跑起來，實際素材需另尋來源 | 階段 1a 可用佔位圖繞過；正式素材時機未定 |
| D-11 | 廣告免費領體力：每次領取幾點？ | 無提案 | 變現版（單機版不需要） |

D-08 **卡在製作人這邊**：需要外部美術來源。單機版階段可用佔位圖完全繞過，不阻塞開發。
D-09 無法紙上決定，必須在手機實機上調到手感對為止。
D-11 只在做變現版時才需要，目前不阻塞。

---

## 四、教訓紀錄

> 同一個錯誤出現第二次即代表缺機制。記錄於此，並轉為規則或自動檢查。

### L-01 [2026-08-15] GitHub Pages 只允許從預設分支部署

**現象**：`deploy` job 在 2 秒內失敗，零個步驟被執行，且無任何 log 可下載。
`build` job（型別檢查、測試、建置、上傳 artifact）全部成功。

**原因**：啟用 Pages 時 GitHub 會自動建立 `github-pages` 環境，其部署分支政策
**預設只允許 repo 的預設分支**。本 repo 建立時是空的，GitHub 把第一個被推上去的
分支 `claude/game-design-beg7xo` 設為預設，因此從 `main` 觸發的部署在 job 啟動前
就被環境保護規則拒絕——這也是為何完全沒有 log。

**機制化措施**：
1. 本檔「部署現況」一節記錄現行唯一可用的部署方式（dispatch + 指定 ref）。
2. `deploy.yml` 保留 `workflow_dispatch`，這是目前唯一能部署的途徑，**不得移除**。
3. 根治方式是把預設分支改為 `main`；未完成前不要以為 push 到 `main` 就會上線。

**判讀原則**：往後若見到某個 job「零步驟、秒失敗、無 log」，優先懷疑環境保護規則
或權限，不要往程式碼方向查。

### L-02 [2026-08-15] 單元測試只驗相對關係，抓不到「參數把畫面畫到螢幕外」

**現象**：2.5D 投影完成後，型別檢查通過、41 項測試全過、建置成功，
但實際開啟畫面只有天空與 HUD——道路、閘門、史萊姆全部不見。

**原因**：`cameraHeight` 是**世界單位**，會再乘上 `scale`，我卻當成像素填了 190。
在 z=14 時 y = 300 + 190×18.6 ≈ 3828，整條路被投影到畫面下方 3800px 處。
公式完全正確，錯的是參數。

**為何測試沒抓到**：當時的投影測試只驗證**相對關係**——越遠越小、左右對稱、
不產生 NaN。這些性質在錯誤參數下**照樣全部成立**。測試從未問過
「投影結果是否真的落在 0..GAME_HEIGHT 之內」。

**機制化措施**：
1. `tests/data.test.ts` 新增「投影參數的可視性」區塊，斷言史萊姆、最遠閘門、
   路面近端與遠端都落在畫面內，並已驗證把 `cameraHeight` 改回 190 時
   會有 3 項測試失敗——確認這組測試不是裝飾品。
2. 同一區塊另以世界座標約束史萊姆不得凸出路面
   （`playerMaxX + playerRadiusWorld ≤ roadHalfWidth`）。

**判讀原則**：涉及座標、尺寸、時間的參數，除了驗證公式的**性質**，
還必須驗證**具體數值落在合理範圍內**。只測性質的測試對數量級錯誤完全免疫。

格式範例：
```
### L-01 [日期] 標題
現象：
原因：
機制化措施：（寫入哪個檔案／加了什麼檢查）
```

---

## 五、已完成紀錄

### 階段 1b（階段 A）— 2.5D 閘門推進（2026-08-15）

**規格變更**：戰鬥畫面改為 2.5D 偽透視，階段 A 的操作由離散左右滑改為按住拖曳。
`GAME_DESIGN.md` 第 3.0／3.2／3.3 節與 `TECH_SPEC.md` 第 4.5／4.6 節已同步更新。

**新增模組**：

| 檔案 | 職責 |
|---|---|
| `src/systems/projection.ts` | 2.5D 投影，純函式，不含 Phaser |
| `src/systems/gateRun.ts` | 閘門推進與判定，純模組，不含 Phaser |
| `src/input/drag.ts` | 相對位移拖曳追蹤，輸出 `x ∈ [-maxX, maxX]` |
| `src/data/types.ts` | zod schema，含 pairs 引用完整性的交叉驗證 |
| `src/data/load.ts` | 載入並驗證資料，格式錯誤在載入階段就報錯 |
| `src/scenes/BattleScene.ts` | 只負責繪製與輸入轉換，不含規則 |
| `data/balance.json` | 投影、推進、拖曳、呈現、關卡閘門數 |
| `data/gates.json` | 閘門類型與配對，8 種閘門、6 組取捨 |

**設計決定**：

- 閘門加成公式為 `(基礎攻擊 + 加法總和) × 乘法總積`，刻意與選擇順序無關。
  玩家是照閘門出現順序被動選擇的，若結果隨順序改變等於引入無法規劃的隱藏規則。
- 帶負面效果的閘門在畫面上以警示色呈現，讓「取捨」在視覺上就成立。
- 顏色不放進 `balance.json`：配色屬美術風格，與調數值的時機完全不同。

三道閘門（TECH_SPEC 第 10.4 條）：

| 閘門 | 結果 |
|---|---|
| `tsc --noEmit` | 通過 |
| `npm test` | 44 項通過 |
| 瀏覽器實際畫面 | 以 Chromium 模擬觸控拖曳實跑，透視道路、閘門逼近、選邊判定、撞牆震動、結算畫面均正確，無 JS 錯誤 |

過程中修正一次嚴重錯誤（見 L-02）：投影參數的量級錯誤使整個畫面被畫到螢幕外，
而當時的測試全部通過。已補上可視性測試並驗證其有效。


### 階段 0 — 專案初始化（2026-08-15）

建置產物：Phaser 3.90.0 / TypeScript 7.0.2 / Vite 8.2.1 / Vitest 4.1.10，全部精確版本並提交 lockfile。

- 目錄骨架依 TECH_SPEC 第 2 節建立（空目錄以 `.gitkeep` 佔位，未預先寫入任何抽象層）
- `tsconfig.json` 開 `strict`，另加 `noUncheckedIndexedAccess`、`exactOptionalPropertyTypes` 等強化項
- `vite.config.ts` 的 `base` 設為 `/SB-Game/`
- `.github/workflows/deploy.yml`：`npm ci` → `tsc --noEmit` → `npm test` → `build` → 部署 Pages
- `src/config.ts` 定義 9:16 基準解析度 540×960；`BootScene` 顯示 Hello 與史萊姆佔位圖

三道閘門（TECH_SPEC 第 10.4 條）驗證結果：

| 閘門 | 結果 |
|---|---|
| `tsc --noEmit` | 通過 |
| `npm test` | 4 項通過 |
| 瀏覽器實際畫面 | 以 Chromium 於 360×640 與 393×852 實測，畫布正確置中縮放、無 JS 錯誤 |

CI 上同樣三道閘門全過（run 31858703956），`deploy` job 成功，GitHub 回報部署狀態
`success`，environment_url 為 <https://bozhan9527-ux.github.io/SB-Game/>。

**與規格的差異（需製作人追認）**：`deploy.yml` 除 TECH_SPEC 第 5 節指定的 `push: main` 外，另加了 `workflow_dispatch` 手動觸發。事後證明這是目前唯一能成功部署的途徑（見 L-01），建議保留。

**尚未驗證**：線上頁面的實際畫面。Claude 所在容器的網路政策封鎖 `*.github.io`
（CONNECT 403），無法自行開啟該網址確認渲染結果——這是環境限制，非程式問題。
本機 Chromium 的渲染已驗證，線上渲染需製作人以手機確認。
