# 遙測（PostHog）。留空或整個檔案不存在時，遊戲完全不連網，
# posthog-js 也不會被打包進去——開發與測試預設就是這個狀態。
#
# 取得方式：PostHog → Project settings → Project API Key（以 phc_ 開頭）。
# 這把 key 本來就是公開的，它會出現在前端的 JS 裡；它只能寫入事件，不能讀取資料。
#
# 本機開發要試的話，把這個檔案複製成 .env.local 再填。
VITE_POSTHOG_KEY=
# 依你的 PostHog 區域：美國 https://us.i.posthog.com、歐盟 https://eu.i.posthog.com
VITE_POSTHOG_HOST=https://us.i.posthog.com

# 後端位址（雲端存檔、排行榜、百分位）。留空時那些畫面整個不出現。
# 本機開發：先起 server/（見 server/README.md），再填 http://localhost:8788
VITE_API_BASE=
