# GAME_DESIGN.md — 《問道飛升》設計規格

> 本檔為玩法規格。技術規範見 `TECH_SPEC.md`，進度見 `PROGRESS.md`。

---

## 0. 一句話定位

選一個門派，帶著門人一路衝關：**手指按住畫面帶隊伍左右走位**，在兩道閘門之間二選一，累積**人數**與**武裝值**，
穿過敵陣後決戰關底**首領**。通關拿金幣，回洞府提升五項屬性，關卡推進則境界跟著提升
（煉氣期 → 築基期 → 金丹期 → 元嬰期 → 化神期 → 煉虛期 → 合體期 → 大乘期 → 渡劫期 → 飛升境）。

平台：手機優先（觸控），網頁版（GitHub Pages）先行，直向 9:16、基準解析度 540×960。

---

## 1. 核心循環

```
選門派 → 挑戰第 N 關 ──通關──→ 結算金幣 → 洞府升級 → 挑戰第 N+1 關
                     └──失敗──→ 拿三成五安慰獎金幣 → 洞府升級 → 重打第 N 關
```

失敗不倒退關卡、不扣屬性，只是原地重來——挫折感低，重打時閘門會換一批（種子含挑戰次數）。

---

## 2. 兩種資源

| 資源 | 意義 | 影響 |
|---|---|---|
| **人數**（弟子） | 隊伍規模 | 隊伍戰力的乘數；被敵陣與首領按比例削減 |
| **武裝值** | 全隊裝備水準 | 提高每名門人的輸出，同時是主要減傷來源 |

隊伍戰力 = `人數 ×（攻擊 + 武裝）×（1 + 境界壓制）`

兩者都不能偏廢：只堆人數會被敵陣按高比例削掉，只堆武裝則人少打不動首領血量。
這是閘門選擇的核心取捨。

---

## 3. 關卡結構（滑動推進）

一關由一串「遭遇」組成，由上而下捲動經過隊伍：

1. **閘門**（主體）：左右各一道，隊伍中心在哪一半就吃哪一側的效果。
   隊伍連續跟著手指移動，可以貼著中線等閘門捲近再決定。
   - 種類：`＋N 弟子`、`×N 弟子`、`＋N 武裝`、`×N 武裝`、`＋N 金幣`
   - 約兩成二的機率其中一側是陷阱（減人數／減武裝），另一側必為好處
   - 兩側都是好處時，盡量給不同資源（人數 vs 武裝），讓選擇有意義
   - 閘門的**加算值隨關卡等比放大**（`gateValueGrowth`），乘算值不放大
2. **敵陣**：每兩道閘門插入一次，橫跨整條路，無法閃避。
   - 造成**比例傷亡**：`傷亡比例 = 威脅 /（威脅 + 攻擊 + 減傷）`
   - 用比例而非固定人數，小隊才不會在半途被一波抹平（關卡難度應由首領決定）
   - 畫面上直接顯示「預估傷亡 −N%」，讓武裝值的價值看得見
   - **陣前齊射**：敵陣進入射程（560 px）後，門人每 0.23 秒朝**正前方射界**（半寬 135 px）
     放出一道靈光，命中的兵卒直接倒下並實際削掉這一排 1/6 的威脅，上限 45%。
     射界有寬度，所以「敵陣還遠的時候就走到人多的那一側」有實質回報——
     走位因此不只用來選閘門，也是減傷手段。命中時直接跳出「少死幾人」的數字
3. **首領**：關底決戰。
   - 血量 `hpBase × hpGrowth^(關卡-1)`，隨關卡指數成長
   - 我方每秒傷害 = `隊伍戰力 × dpsFactor × 對首領傷害倍率 ×（1 + 氣勢）`
   - 首領每 1.1 秒反擊一次，同樣是**比例傷亡**（避免「戰力雪崩」，見第 7 節）
   - **氣勢**：持續左右晃動可累積傷害加成（上限 +60%），停手會自然衰退
   - **猛攻 / 守勢**：手指停住不動超過 0.42 秒即轉為守勢——**輸出砍半，但挨打也砍半**。
     隊伍快被磨光時停手保命、血量健康時猛攻速殺，首領戰因此有了真正的取捨
   - 40 秒時限內沒打完視為失敗
   - 氣勢在畫面上是 `×1.00` 的大字倍率讀數（底下才是長條）：長條只看得出多寡，
     倍率才看得出「再晃值不值得」

閘門捲動速度隨關卡加快（165 → 380 px/秒上限），閘門數隨境界增加（6 → 14 道），
每兩道閘門插一次敵陣，因此一關約 8–20 個遭遇。

**連擊**：連續吃到好處的閘門會累積連擊層數，踩到陷阱歸零（符修的陷阱免疫連帶保住連擊）。
連擊**只放大金幣（每層 +10%，上限 10 層）與首領戰的開場氣勢**，不放大當場的人數與武裝——
它是「這一場走得多乾淨」的計分板，不是又一條讓本關變簡單的捷徑。
層數以左上角的倍率牌呈現，每疊一層彈一下，斷掉時整塊消失。

**傷害數字**：所有收益與傷亡都以浮動數字長在發生的座標上（閘門收益在隊伍頭上、
齊射的戰果在被打倒的兵卒身上、我方輸出在首領身上），而不是集中成一行字。
出手的節奏因此讀得出來。

---

## 4. 門派（開局選擇，可隨時更換）

| 門派 | 路數 | 數值 | 被動（改變玩法決策） |
|---|---|---|---|
| 天罡宗 | 體修 | 人數+4、防禦+6、敵陣傷亡 ×0.7 | **銅皮鐵骨**：每關前兩次敵陣完全免傷 |
| 青霄劍宗 | 劍修 | 攻擊+5、首領傷害 ×1.35、人數-1 | **一劍破萬法**：首領戰開場氣勢全滿，但氣勢衰退加倍 |
| 玄符門 | 符修 | 武裝效果 ×1.5、攻防各+2 | **符籙鎮邪**：踩到陷阱閘門完全無效 |
| 百草丹閣 | 丹修 | 金幣 ×1.5、敵陣傷亡 ×0.85 | **丹藥回春**：每通過一道金幣閘門回復一成門人 |

被動是刻意設計成**改變決策**而不只是數值差異：

- 體修可以先放心堆人數，不必一開場就顧武裝
- 劍修必須速戰速決，拖久了氣勢掉光
- 符修可以無視陷阱側，選擇邏輯與其他門派完全不同
- 丹修的金幣閘門兼具補血，「拿錢」與「保人」不再互斥

---

## 5. 境界

關卡編號決定境界，**每個境界九層**，一輪十個境界共 81 關，第 82 關之後為無限的飛升境。
境界提供**境界壓制**（隊伍戰力加成，煉氣 0% → 飛升 +90%），並決定敵陣與首領的名稱。

| 境界 | 關卡 | 境界 | 關卡 |
|---|---|---|---|
| 煉氣期 | 1–9 | 煉虛期 | 46–54 |
| 築基期 | 10–18 | 合體期 | 55–63 |
| 金丹期 | 19–27 | 大乘期 | 64–72 |
| 元嬰期 | 28–36 | 渡劫期 | 73–81 |
| 化神期 | 37–45 | 飛升境 | 82+ |

---

## 6. 金幣升級（洞府）

五條線，花費逐級遞增（`baseCost × costGrowth^等級`）：

| 項目 | 效果 | 每級 |
|---|---|---|
| 聚眾成軍 | 起始人數與所有人數閘門的收益 | +8% |
| 護體罡氣 | 減傷（敵陣與首領的傷亡比例下降） | +8% |
| 淬鍊功法 | 全隊攻擊（單兵戰力） | +8% |
| 聚寶之術 | 金幣蒐集量 | +10% |
| 斬妖訣 | 對首領傷害 | +10% |

**五條線一律是百分比乘算。** 早期版本用固定加值（起始人數 +1、起始攻擊 +1…），
量測結果是「多買三級完全不影響能推到第幾關」——閘門收益隨關卡等比放大，
固定加值在後期會被稀釋到看不見。金幣獎勵同樣改為等比成長，
否則指數的升級花費配上線性的收入，中後期根本買不起。

---

## 7. 三個設計決定（都是模擬跑出來的結論）

**敵陣與首領都用比例傷亡，不用固定人數。**
固定人數的版本在模擬中會出現兩種失敗：運氣差的小隊在半途被一波敵陣抹平；
首領戰打掉一半人之後輸出也砍半，變成「前幾秒沒殺死就必輸」的雪崩，
玩家只能看著隊伍融化卻無事可做。改成比例後人數平滑衰減，戰鬥維持在「快一點就贏得下來」的張力。

**閘門數值隨關卡連續成長，不分階。**
早期版本把閘門分成四個階層、每兩個境界跳一階，結果第 13 關戰力暴增 6 倍、首領被 0.6 秒秒殺，
而下一階之前又完全打不動。改成加算值等比成長後，隊伍規模與首領血量走在同一條曲線上。

**連擊只放大金幣，不放大戰力。**
連擊放大戰力的版本在模擬中直接把本關難度抹平：走得順的那一場會越滾越大，
走得不順的那一場則毫無補救。改成只放大金幣（長期成長）與開場氣勢（首領戰的起跑優勢）後，
它變成獎勵而不是難度開關。陣前齊射同理有 45% 的削弱上限：再怎麼走位也不可能無傷穿過敵陣。

調校結果（見 `tests/balance.test.ts`）：四個門派都能以平均**約 1.2 場**的代價推進一關，
最壞的一關重打不超過 6 次，九成的首領戰落在 2–25 秒。
模擬時齊射只取上限的一半（`volleyMaxWeaken × 0.5`），代表「會用但沒用好」的普通玩家，
因此模擬結果對實際玩家是偏保守的。

---

## 8. 資料檔

所有數值在 `data/`，`src/` 不硬編碼（TECH_SPEC 第 3 節）：

| 檔案 | 內容 |
|---|---|
| `balance.json` | 滑動門檻、捲動、戰力公式係數、敵陣、首領、金幣 |
| `realms.json` | 十個境界的關卡區間、配色、境界壓制 |
| `sects.json` | 四個門派的加成與乘區 |
| `gates.json` | 閘門模板（資源、加算/乘算、基準值、權重、是否陷阱） |
| `upgrades.json` | 五條升級線的效果與花費曲線 |
| `enemies.json` | 各境界的敵陣與首領名稱、首領台詞 |

`src/` 內只剩畫面座標（車道位置、面板大小）這類呈現參數。

---

## 9. 美術與音效

兩者都不使用外部素材，全部由程式產生。

**美術：手寫 SVG**（`public/art/`，載入時由 Phaser 點陣化）

| 檔案 | 用途 |
|---|---|
| `disciple-{門派}-t{階}-{0,1}.svg` | 四個門派 × 三階 × 兩幀。體修橘袍纏巾握拳、劍修青袍佩長劍、符修紫袍兜帽持符、丹修綠袍懸葫蘆舉藥草。**全彩繪製**，不用 `setTint` 染色 |
| `enemy-{類型}-{0,1}.svg` | 十種敵陣各一套兩幀。**妖獸六種各自畫成名字裡的生物**：妖狼（尖耳長吻蓬尾）、妖熊（圓耳厚身）、雪怪（雙足白毛長臂）、蜈蚣（多節多足帶觸鬚）、火蠍（雙螯翹尾毒針）、蟒蛟（S 形環節吐信）；人形四種：流寇蒙面舉刀、屍傀僵臂貼符、魔修尖角肩刺、天兵羽盔長戟。每筆敵陣在 `enemies.json` 指定用哪一種 |
| `gate-arch.svg` | 閘門的山門牌坊。**不鋪底色**，只用線條與文字的顏色區分資源種類（弟子綠／武裝藍／金幣金／陷阱紅） |
| `boss-beast/demon/storm/celestial.svg` | 四種首領造型，每個首領在 `enemies.json` 指定用哪一種。**全彩繪製**，境界色只保留在身後的光暈。待機時起伏擺動，出手前蓄力後撲下，中招時閃白縮身 |
| `cloud.svg` | 背景的祥雲，三層不同速度橫移 |
| `slash.svg` | 首領戰的劍氣，氣勢越高閃現越密 |

用 SVG 而非點陣圖的理由：檔案小、任意縮放不糊、可用程式產生。
人物圖由 `scripts/gen_art.py` 產生——兩幀之間只有四肢差異、其餘完全一致，
手寫很難維持這種一致性，改配色也只要動一個色表。

**走路動畫**由兩張獨立貼圖組成（Phaser 的 `anims` 允許 `frames` 直接列貼圖 key，不需要 spritesheet）。
群像中每個人的動畫進度以自己的相位錯開，整團才不會像同一個人複製了三十份。

**門人造型隨境界提升**，同一個門派共三階：

| 階 | 境界 | 外觀 |
|---|---|---|
| 初階 | 煉氣 – 金丹 | 素袍，僅門派本色 |
| 中階 | 元嬰 – 煉虛 | 加金邊披肩與下擺滾邊，配色轉亮 |
| 高階 | 合體 – 飛升 | 再加披風、頭冠與周身靈光，門派專屬配件也升級（劍修多一柄御劍、符修三張符、丹修環繞丹丸、體修護腕）|

**音效：WebAudio 即時合成**（`src/audio/`，零音檔）

- 撥弦用 Karplus-Strong（噪音短脈衝丟進延遲迴路），音色接近古箏／琵琶，高音自然衰減得比低音快
- 鑼用六個非諧和分音相加，用在通關、道消、突破境界
- 風聲與衝擊用白噪音加包絡，再過帶通濾波器掃頻
- 悶響用正弦音高下滑，用在陷阱閘門與首領重擊
- 配樂是五聲音階（宮商角徵羽）的八拍樂句，境界越高主音越高、樂句跟著換；
  以 AudioContext 時間軸逐小節排程，掉幀時節奏不會亂

音準有測試把關：`tests/audio.test.ts` 用自相關量測合成波形的基頻，
220/440/660 Hz 都要落在 ±5% 內。

瀏覽器規定要有使用者手勢才能發聲，因此第一次點擊前是靜默的。
音效開關在標題頁右上角，狀態存進存檔（`settings.sound`）。

---

## 10. 成就（仙途錄）

16 條長期目標，條件寫在 `data/achievements.json`，達成即發金幣：
抵達各境界（8 條）、單場人數／武裝門檻、3 秒內斬殺首領、累計通關次數、
累計金幣、四個門派各通關一次。

判定只看存檔裡的原始事實（最高關卡、累計通關、單場最高人數…），不存衍生值；
要調門檻只改 JSON。結算畫面達成時會浮出提示，標題頁的「仙途錄」可看全部進度。

---

## 11. 尚未處理

- **美術風格的上限**：手寫 SVG 只適合做單色剪影，做不出彩繪立繪。要更精緻的美術仍需外部來源。
- **音色的上限**：程式合成的音色偏簡樸，沒有真實樂器的層次；也沒有環境音與人聲。
- **手機實機手感**：滑動門檻（40 px / 300 ms）與捲動速度只在桌機瀏覽器驗過，需要真機再調。
- **飛升境之後**：第 28 關以後境界不再變化，只有數字繼續長，還沒有收尾設計。
