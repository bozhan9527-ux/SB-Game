# GAME_DESIGN.md — 《問道飛升》設計規格

> 本檔為玩法規格。技術規範見 `TECH_SPEC.md`，進度見 `PROGRESS.md`。

---

## 0. 一句話定位

選一個門派鎮守山門：妖魔由畫面上方一波波推進，玩家把手牌的**法寶符**拖到陣位上，
符會自動朝上出手；**同種同階的兩張符疊在一起就合成高一階**，傷害是指數成長的。
擋不住就會有妖魔攻進山門，扣光耐久即失守。關底是一隻血厚的**首領**。
通關拿金幣，回洞府提升六項屬性，關卡推進則境界跟著提升
（煉氣期 → 築基期 → 金丹期 → 元嬰期 → 化神期 → 煉虛期 → 合體期 → 大乘期 → 渡劫期 → 飛升境）。

平台：手機優先（觸控拖曳），網頁版（GitHub Pages）先行，直向 9:16、基準解析度 540×960。

---

## 1. 核心循環

```
選門派 → 守第 N 關 ──守住──→ 結算金幣 → 洞府升級 → 守第 N+1 關
                   └──失守──→ 拿三成五安慰獎金幣 → 洞府升級 → 重守第 N 關
```

一場之內還有一層更短的循環，這才是玩家真正在做的事：

```
抽到符 → 放上陣位（立刻有輸出） 或 留在手裡湊一對（之後翻倍）
      → 合成 → 陣位空出來 → 再抽
```

失敗不倒退關卡、不扣屬性，只是原地重來——重打時妖魔與抽到的符會換一批（種子含挑戰次數）。

---

## 2. 兩種資源

| 資源 | 意義 | 影響 |
|---|---|---|
| **山門耐久**（弟子） | 你能被攻進幾次 | 每漏一隻扣一次，歸零即失守；代價隨關卡成長 |
| **法寶符** | 場上的輸出 | 傷害 = `基礎 × 階數成長^(階-1) × 各項倍率`，階數是指數 |

**階數上限每三關 +1**（煉氣期第 1 關是 4 階，第 81 關是 30 階）。
這是全遊戲唯一的指數成長來源：上限每往上跳一階，輸出天花板就乘上 `tierGrowth`。
抽到的符固定比上限低四階，所以要把一張符推到上限，得合十六張——**抽符速度就是成長速度**。

---

## 3. 一關的結構

- **五波妖魔**，每波間隔 9 秒，波內的出場時間攤在整個波長上（不是一次湧出後空場）
- 每波比前一波多、也比前一波硬；妖魔沿五條縱列往下走，走到山門就是漏怪
- 第五波之後出**首領**：血量是全關的三到五成，推進得慢但撞進山門要扣六倍耐久
- 首領出場後有 60 秒時限，逾時視為失守

**四種法寶符**（`data/cards.json`），差異在「一次打幾個」與「多久打一次」：

| 符 | 傷害 | 間隔 | 目標數 | 定位 |
|---|---|---|---|---|
| 劍陣符 | 11 | 0.52s | 1 | 出手快、單體穩，斬首領最可靠 |
| 天雷符 | 40 | 1.50s | 1 | 單體重擊。打小妖會**溢傷浪費**，打首領最狠 |
| 風刃符 | 8 | 0.62s | 3 | 清雜兵最快，對首領最弱 |
| 烈焰符 | 15 | 0.80s | 2 | 攻守之間最平衡 |

**溢傷是真的**：傷害逐目標結算、超出的部分直接丟掉。
天雷符一擊 40 點打在 20 血的小妖身上會浪費一半，風刃符幾乎不浪費——
四種符的取捨是從規則長出來的，不是寫在說明文字裡的形容詞。

**拖放只有一個入口**：能合就合、空位就搬、否則互換（手牌之間、手牌↔陣位、陣位之間都一樣）。
拖到畫面最下緣是棄符。收斂成一種手勢是刻意的——若「合成／放置／換位」各有各的條件，
玩家會遇到「拖了半天什麼都沒發生」，那是這類遊戲最容易勸退人的地方。

**陣法**：同一種符（階數不必相同）連成一整條線就成陣，這是格子唯一的意義。

| 陣 | 條件 | 效果 |
|---|---|---|
| 橫陣 | 一整橫列同種 | 該列傷害 +22% |
| 縱陣 | 一整直行同種 | 該行出手速度 +25% |
| 斜陣 | 對角線同種（需 3×3，即陣法擴充買滿） | 傷害 +34% |

同時落在橫陣與縱陣上的那一格，兩種加成相加（十字）。
**條件是「同種」而不是「同階」**：同階的兩張本來就該合成，用同階當條件等於在懲罰合成；
用同種當條件才會形成真正的取捨——合成會空出一格、把線打斷，
所以「現在合，還是先讓這一列成陣」是每隔幾秒就要做一次的決定。

三種加成都刻意**小於一階的成長**（`tierGrowth` 1.35）。陣法是「排得好有賞」，
不能大到讓「為了排陣而不敢合成」變成最佳解——長期而言唯一跟得上難度的還是合成。
斜陣同時是「陣法擴充」這條升級線的獎勵：買滿才有 3×3 的格子可以排。

---

## 3b. 新手引導

**第一次進第 1 關會跑互動教學**（存檔的 `player.hints` 記錄看過沒有，老玩家不會被打斷）。
教學只有三步，而且**做到才往下走**，不用計時器——新手花多久摸索都可以：

1. 「把符放上陣位」——戰鬥暫停，等玩家把一張符拖到空格
2. 「同種同階可以合成」——戰鬥仍暫停，等玩家把第二張疊上去
3. 「守住山門」——恢復戰鬥，讀完三秒後教學結束

教學用的是**固定起手牌**（場上全空、手上三張同種同階的烈焰符），不是隨機抽的。
隨機起手有可能三張都合不起來，第二步就卡死了；教學只有一次機會，不能靠運氣。

另外有兩則**一次性提示**，觸發時機到了才出現、看過就不再出現：
手牌塞滿導致抽到的符流失時提醒可以棄符；首領第一次出場時提醒它的代價是六倍耐久。

主畫面的「玩法說明」是可隨時查閱的完整規則（目標／拖放三條規則／合成與階數上限／
四種符的取捨／門派／洞府）。頁面裡的數字全部從 `data/*.json` 讀，不另抄一份——
抄一份就一定會和實際數值走散。

---

## 4. 門派（開局選擇，可隨時更換）

| 門派 | 路數 | 數值 | 被動（改變玩法決策） |
|---|---|---|---|
| 天罡宗 | 體修 | 山門 ×1.5 | **銅皮鐵骨**：每關前兩次漏怪完全免傷 |
| 青霄劍宗 | 劍修 | 山門 ×0.85、首領傷害 ×1.2 | **一劍破萬法**：劍陣符傷害 +35% |
| 玄符門 | 符修 | 無 | **符籙相生**：合成有 30% 機率保留來源那一張符 |
| 百草丹閣 | 丹修 | 金幣 ×1.5、法寶傷害 ×0.9 | **丹藥催符**：抽符速度 +25% |

被動是刻意設計成**改變決策**而不只是數值差異：

- 體修可以放心讓前兩隻走過去，專心先合階數
- 劍修一路押注劍陣符，其他符只是過渡
- 符修的合成成本更低，敢在手裡湊高階牌而不怕抽不到下一張
- 丹修抽得快，靠數量堆階數，也最快把金幣滾起來

---

## 5. 境界

關卡編號決定境界，**每個境界九層**，一輪十個境界共 81 關，第 82 關之後為無限的飛升境。
境界提供**境界壓制**（法寶傷害加成，煉氣 0% → 飛升 +90%），並決定妖魔與首領的名稱與背景地貌。

| 境界 | 關卡 | 境界 | 關卡 |
|---|---|---|---|
| 煉氣期 | 1–9 | 煉虛期 | 46–54 |
| 築基期 | 10–18 | 合體期 | 55–63 |
| 金丹期 | 19–27 | 大乘期 | 64–72 |
| 元嬰期 | 28–36 | 渡劫期 | 73–81 |
| 化神期 | 37–45 | 飛升境 | 82+ |

---

## 6. 金幣升級（洞府）

六條線，花費逐級遞增（`baseCost × costGrowth^等級`）：

| 項目 | 效果 | 每級 |
|---|---|---|
| 聚眾成軍 | 山門耐久上限 | +8% |
| 御器訣 | 所有法寶的出手速度 | +6% |
| 淬鍊功法 | 所有法寶的傷害 | +8% |
| 引靈訣 | 抽符速度 | +6% |
| 聚寶之術 | 所有金幣收益 | +10% |
| 陣法擴充 | 場上多一個陣位（上限 +3） | +1 格 |

除了陣法擴充，其餘一律是**百分比乘算**。原因見第 7 節與 PROGRESS 的 L-05。

`tests/balance.test.ts` 對每一條線都驗過「在快打不過的那一關，買了它勝率會上升」——
量的是玩家真正在乎的事，而不是帳面數字有沒有變大。

---

## 7. 四個設計決定（都是模擬跑出來的結論）

**階數上限每三關 +1，而不是每個境界 +1。**
每境界（九關）+1 的版本在模擬中變成鋸齒：同一境界內難度漲 2.4 倍而玩家完全不成長，
到了境界末尾打不動，一突破又輾壓。週期縮短到三關之後，難度與成長走在同一條線上。

**合成只在同種同階之間成立，而且手牌之間也能合。**
只能合在場上的版本會讓「抽到和場上對不上的符」變成純粹的損失，玩家沒辦法先湊一對再放下去，
決策層少了一半。允許手牌合成之後，「先擺著出輸出」與「留在手裡等對子」成為每次抽符都要做的取捨。

**漏怪的代價隨關卡成長。**
代價固定的版本裡，「聚眾成軍」會一路把耐久堆到吃不完，整個系統退化成「買耐久就贏」——
模擬顯示它一條線就能把邊界關卡往後推 16 關，其他五條加起來還沒它多。
代價跟著關卡長之後，它只是「多一點容錯」。

**斬妖訣（對首領傷害）整條砍掉，換成陣法擴充。**
模擬顯示邊界關卡幾乎都輸在雜兵漏怪、而不是首領打不動，
所以「對首領傷害 +80%」推不動任何一關——它在這個類型裡就是條死線。
換成「多一個陣位」之後，它是全遊戲唯一的結構性升級，而且立刻看得到。

調校結果（見 `tests/balance.test.ts`）：四個門派都能一路推到第 81 關，
零升級會在第 20 關上下撞牆，全程約兩成的關卡會漏怪（守得住不代表守得輕鬆）。

---

## 8. 資料檔

所有數值在 `data/`，`src/` 不硬編碼（TECH_SPEC 第 3 節）：

| 檔案 | 內容 |
|---|---|
| `balance.json` | 手牌與陣位容量、階數曲線、抽符節奏、波次編成、首領、金幣 |
| `realms.json` | 十個境界的關卡區間、配色、境界壓制、地貌 |
| `cards.json` | 四種法寶符的傷害、出手間隔、目標數、抽取權重 |
| `sects.json` | 四個門派的乘區與被動 |
| `upgrades.json` | 六條升級線的效果與花費曲線 |
| `enemies.json` | 各境界的妖魔與首領名稱、首領台詞、造型 |

`src/` 內只剩畫面座標（陣位位置、面板大小）這類呈現參數。

---

## 9. 美術與音效

兩者都不使用外部素材，全部由程式產生。

**美術：手寫 SVG**（`public/art/`，載入時由 Phaser 點陣化）

| 檔案 | 用途 |
|---|---|
| `disciple-{門派}-t{階}-{0,1}.svg` | 四個門派 × 三階 × 兩幀。體修橘袍纏巾握拳、劍修青袍佩長劍、符修紫袍兜帽持符、丹修綠袍懸葫蘆舉藥草。**全彩繪製**，不用 `setTint` 染色 |
| `enemy-{類型}-{0,1}.svg` | 十種敵陣各一套兩幀。**妖獸六種各自畫成名字裡的生物**：妖狼（尖耳長吻蓬尾）、妖熊（圓耳厚身）、雪怪（雙足白毛長臂）、蜈蚣（多節多足帶觸鬚）、火蠍（雙螯翹尾毒針）、蟒蛟（S 形環節吐信）；人形四種：流寇蒙面舉刀、屍傀僵臂貼符、魔修尖角肩刺、天兵羽盔長戟。每筆敵陣在 `enemies.json` 指定用哪一種 |
| `glyph-{符種}.svg` | 四種法寶符的圖騰（劍、雷、風、火），畫在牌面上方。只畫輪廓、留白多，因為它在手牌上只有二十來個像素高 |
| `boss-beast/demon/storm/celestial.svg` | 四種首領造型，每個首領在 `enemies.json` 指定用哪一種。**全彩繪製**，境界色只保留在身後的光暈。待機時起伏擺動，出手前蓄力後撲下，中招時閃白縮身 |
| `cloud.svg` | 背景的祥雲，三層不同速度橫移 |
| `slash.svg` | 命中特效 |

用 SVG 而非點陣圖的理由：檔案小、任意縮放不糊、可用程式產生。
人物圖由 `scripts/gen_art.py` 產生——兩幀之間只有四肢差異、其餘完全一致，
手寫很難維持這種一致性，改配色也只要動一個色表。

**走路動畫**由兩張獨立貼圖組成（Phaser 的 `anims` 允許 `frames` 直接列貼圖 key，不需要 spritesheet）。
群像中每個人的動畫進度以自己的相位錯開，整團才不會像同一個人複製了三十份。

門人現在只出現在山門兩側（守門）與門派選擇畫面，但造型仍**隨境界提升**，同一個門派共三階：

| 階 | 境界 | 外觀 |
|---|---|---|
| 初階 | 煉氣 – 金丹 | 素袍，僅門派本色 |
| 中階 | 元嬰 – 煉虛 | 加金邊披肩與下擺滾邊，配色轉亮 |
| 高階 | 合體 – 飛升 | 再加披風、頭冠與周身靈光，門派專屬配件也升級（劍修多一柄御劍、符修三張符、丹修環繞丹丸、體修護腕）|

**音效：WebAudio 即時合成**（`src/audio/`，零音檔）

- 撥弦用 Karplus-Strong（噪音短脈衝丟進延遲迴路），音色接近古箏／琵琶，高音自然衰減得比低音快
- 鑼用六個非諧和分音相加，用在通關、道消、突破境界
- 風聲與衝擊用白噪音加包絡，再過帶通濾波器掃頻
- 悶響用正弦音高下滑，用在陷阱閘門與首領重擊
- 配樂是五聲音階（宮商角徵羽）的八拍樂句，境界越高主音越高、樂句跟著換；
  以 AudioContext 時間軸逐小節排程，掉幀時節奏不會亂

音準有測試把關：`tests/audio.test.ts` 用自相關量測合成波形的基頻，
220/440/660 Hz 都要落在 ±5% 內。

瀏覽器規定要有使用者手勢才能發聲，因此第一次點擊前是靜默的。
音效開關在標題頁右上角，狀態存進存檔（`settings.sound`）。

---

## 10. 成就（仙途錄）

16 條長期目標，條件寫在 `data/achievements.json`，達成即發金幣：
抵達各境界（8 條）、單場合成到六／九階、累計斬殺數、零漏怪通關、累計通關次數、
累計金幣、四個門派各通關一次。

判定只看存檔裡的原始事實（最高關卡、累計通關、單場最高階數…），不存衍生值；
要調門檻只改 JSON。結算畫面達成時會浮出提示，標題頁的「仙途錄」可看全部進度。

---

## 11. 尚未處理

- **美術風格的上限**：手寫 SVG 只適合做單色剪影，做不出彩繪立繪。要更精緻的美術仍需外部來源。
- **音色的上限**：程式合成的音色偏簡樸，沒有真實樂器的層次；也沒有環境音與人聲。
- **手機實機手感**：拖曳的判定範圍與陣位大小只在桌機瀏覽器驗過，需要真機再調。
- **飛升境之後**：第 82 關以後境界不再變化，只有數字繼續長，還沒有收尾設計。
- **舊存檔的斬妖訣等級**：v3 → v4 遷移保留金幣、關卡與其餘升級等級，但「斬妖訣」這條線
  在新玩法裡不存在，其等級不會被退費。改玩法時的一次性取捨，已記在 PROGRESS。
